// Root build file. AGP 9 ships built-in Kotlin support, so no separate
// org.jetbrains.kotlin.android plugin is declared (AGP refuses it).
plugins {
    id("com.android.application") version "9.0.1" apply false
}
