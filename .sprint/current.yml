# Active sprint manifest — CICD-BASELINE-REVERIFY-1
# Validated by: php artisan sprint:manifest-check

id: CICD-BASELINE-REVERIFY-1
type: INFRA_RELEASE
module: Cicd
title: "Full-Suite baseline revalidation & stale failure-debt closure — the historical nine Full Suite failures are reconciled one by one, proven closed by CICD-FIX-6, and the expected failure baseline is retired to zero with the gate left fail-closed"
base_branch: feature/sprint-26-phase-26-8-stabilization-closure-go-watch-no-go-report

runtime_change: false           # no application, config, script or workflow behaviour changes
schema_change: false            # no migration
frontend_change: false          # no JS/CSS/Vite change
security_impact: false          # no policy, permission, middleware or authorization surface touched

deploy_required: false          # tests, docs and rules only — nothing the VPS runtime executes
browser_required: false         # no user-facing surface

test_profiles:
  - cicd

# Declared, not yet created. The tag is only cut after the post-merge Full Suite
# — the canonical authority for this workstream — has actually completed green
# on the merge SHA.
go_tag: cicd-baseline-reverify-1-full-suite-baseline-stale-failure-debt-closure-go
