# Active sprint manifest — LEGACY-RME-PDF-ROLL-4-WAVE-1
# Validated by: php artisan sprint:manifest-check

id: LEGACY-RME-PDF-ROLL-4-WAVE-1
type: DOCS_ONLY
module: LegacyRme
title: "Controlled production migration Wave-1 (LDK2) — executed: 1 genuine document published under a maker/checker split, reconciled clean, capability returned OFF"
base_branch: feature/sprint-26-phase-26-8-stabilization-closure-go-watch-no-go-report

runtime_change: false           # this commit is evidence only; the RBAC change shipped in 17d5ccf
schema_change: false            # no migration, no table, no column
frontend_change: false          # no Blade view, no JS/CSS, no Vite entry
security_impact: false          # the permission grant landed in 17d5ccf, not here
branch_isolation_impact: false  # no BranchContext / branch-scoped query change
ledger_impact: false            # no inventory ledger / billing / payment behaviour change

deploy_required: false          # documentation and governance evidence only
browser_required: false         # no UI surface

test_profiles:
  - docs

# Outcome: GO. Wave-1 ran end to end on production under runtime authority
# 17d5ccfeec18cdf93ca1393520dd1442f837f814.
#
#   accepted 1 / published 1 (ceiling 5)   unexplained 0   quota drift 0
#   imported_by 7 != published_by 11        zero native side effects
#   final state: capability OFF, admission EMPTY, active wave NONE
#
# Not proven live, and deliberately not faked:
#   - quota exhaustion (only one genuine candidate existed)
#   - the authorized-doctor read ON RM 22681 (patient 40 has no native visit, so
#     no doctor has a treating relationship; the positive read is shown on the
#     pre-existing LDK2 record 4 instead)
#
# Evidence in
# docs/sprints/legacy-rme-pdf-roll-4-wave-1-controlled-production-migration.md §7–§9
