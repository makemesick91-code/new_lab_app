# Active sprint manifest — LEGACY-RME-DATE-TZ-1
# Validated by: php artisan sprint:manifest-check

id: LEGACY-RME-DATE-TZ-1
type: RUNTIME_FIX
module: Clinical
title: "Canonical clinical timezone & date-boundary correctness — clinical eligibility must resolve 'today' on the clinic's own calendar (Asia/Makassar), never a silently UTC-defaulted one; declare the clinical calendar once, fail closed on an invalid value, and keep technical instants on their existing UTC architecture"
base_branch: feature/sprint-26-phase-26-8-stabilization-closure-go-watch-no-go-report

runtime_change: true            # ClinicalClock is a new clinical decision input
schema_change: false            # no migration, no table, no column, no data backfill
frontend_change: false          # no Blade view, no JS/CSS, no Vite entry
security_impact: true           # clinical timezone is server-authoritative and must never be request-controlled
branch_isolation_impact: false  # no BranchContext / branch-scoped query change
ledger_impact: false            # no inventory ledger / billing / payment behaviour change

deploy_required: true           # the effective clinical calendar must be proven on the production host
browser_required: false         # no UI surface

test_profiles:
  - legacy_rme
  - architecture
