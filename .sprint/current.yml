# Active sprint manifest — FEATURE-DOCTOR-TRUSTED-ANDROID-DEVICE-LOCK-1 Phase 1
# Validated by: php artisan sprint:manifest-check

id: FEATURE-DOCTOR-TRUSTED-ANDROID-DEVICE-LOCK-1-PHASE-1
type: MODULE_SPRINT
module: RME
title: "Doctor room-scoped active patient isolation + Doctor RME/Odontogram print denial (device-independent half of the trusted-device programme; no Android, no device registry, no login lock)"
base_branch: feature/sprint-26-phase-26-8-stabilization-closure-go-watch-no-go-report

runtime_change: true            # new DoctorRoomScopeService + policy/controller/list-scope guards on live RME surfaces
schema_change: false            # no migration, no table, no column — room context already exists (trx_user_online_contexts)
frontend_change: true           # room-worklist Blade drops the room selector for a scoped Doctor (no JS/CSS/Vite entry)
security_impact: true           # authorization boundary: room isolation, IDOR denial, print denial, audit
branch_isolation_impact: false  # branch scope untouched; room scope narrows strictly inside the existing branch scope
ledger_impact: false            # no inventory ledger / billing / payment behaviour change

deploy_required: true           # live clinical behaviour change on the VPS pilot
browser_required: false         # server-rendered Blade; covered by feature tests

test_profiles:
  - rme
  - security
