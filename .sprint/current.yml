# Active sprint manifest — LEGACY-RME-MASTERDATA-1
# Validated by: php artisan sprint:manifest-check

id: LEGACY-RME-MASTERDATA-1
type: MODULE_SPRINT
module: LegacyRme
title: "RM 27541 patient resolution investigation & master-data integrity — exact identity only, a canonical read-only resolution audit replacing ad-hoc SQL, and a durable disposition for a document that maps to no patient"
base_branch: feature/sprint-26-phase-26-8-stabilization-closure-go-watch-no-go-report

runtime_change: true            # new read-only service, support value object and artisan command
schema_change: false            # no migration, no table, no column, no index
frontend_change: false          # no Blade, JS, CSS, Vite entry or build file
security_impact: false          # no policy, middleware, permission or role change; read-only surface
branch_isolation_impact: false  # branch scope resolution untouched — branch is still derived from the Nomor RM
ledger_impact: false            # no inventory ledger / billing / payment behaviour change

deploy_required: true           # the audit command must exist on production to be runnable there
browser_required: false         # CLI-only surface, covered by feature tests

test_profiles:
  - legacy_rme

go_tag: legacy-rme-masterdata-1-rm-27541-patient-resolution-integrity-go
