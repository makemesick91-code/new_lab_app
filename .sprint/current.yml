# Active sprint manifest — LAB-PROD-2
# Validated by: php artisan sprint:manifest-check

id: LAB-PROD-2
type: MODULE_SPRINT
module: Lab
title: "Lab Workflow V2 Operational Analytics & KPI"
base_branch: feature/sprint-26-phase-26-8-stabilization-closure-go-watch-no-go-report

runtime_change: true            # new analytics service/repository/controller/commands + read-only KPI surface
schema_change: false            # no migration — reads canonical Lab Workflow V2 tables only
frontend_change: false          # Blade + x-ui only; no JS/CSS/Vite change
security_impact: true           # 2 new lab analytics permissions + server-side scope tiers (IDOR-safe)
branch_isolation_impact: true   # analytics is branch/technician scoped via BranchService; owner cross-branch read-only
ledger_impact: false            # inventory ledger untouched; read-only over lab workflow data

deploy_required: true
browser_required: false         # authenticated-equivalent HTTP smoke (route 200/403, CSV export)

test_profiles:
  - lab_workflow
  - access_control
  - branch_context
  - deployment

go_tag: lab-prod-2-operational-analytics-kpi-go
