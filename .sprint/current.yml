### Active sprint manifest — DOCTOR-PWA-GLOBAL-ROLLOUT-READINESS-1
### Validated by: php artisan sprint:manifest-check

id: DOCTOR-PWA-GLOBAL-ROLLOUT-READINESS-1
type: SECURITY_FIX
module: DoctorDevice
title: "Measure whether fleet-wide doctor enforcement could be switched on without stranding a clinician: a read-only per-doctor readiness engine over the whole fleet, a preparation gate that stopped failing the live pilot it was describing, a global line that is measured rather than recorded, and the one still-safe control salvaged from the closed PHASE4A activation pull request"
base_branch: feature/sprint-26-phase-26-8-stabilization-closure-go-watch-no-go-report

runtime_change: true            # a new artisan command, a new module service and
                                # repository, a narrowed check in the preparation
                                # scanner, and a CommandStarting listener. All of it
                                # read-only or refusing; none of it on a clinical path.
schema_change: false            # no migration, no column, no data touched
frontend_change: false          # no JS, no CSS, no Blade
security_impact: true           # it changes a security gate's verdict, adds a runtime
                                # console refusal, and reports the enforcement posture
                                # that decides whether a doctor keeps browser login
branch_isolation_impact: false  # branch is read from the device row for reporting only;
                                # nothing here sets, consults or trusts a request branch
ledger_impact: false            # no inventory ledger / billing / payment behaviour

deploy_required: true           # the readiness command and the console guard only exist
                                # on the deployment that carries them, and the whole
                                # point of the engine is to be run against production
browser_required: false         # nothing here has a UI surface, and nothing here can be
                                # proven by a ceremony: it MEASURES provisioning rather
                                # than performing any. The twelve doctors who are not
                                # ready need physical ceremonies, and this sprint
                                # deliberately claims none of them.

go_tag: doctor-pwa-global-rollout-readiness-1-go

test_profiles:
  - security

# Carried forward. `.sprint/current.yml` is a ROLLING file, so these inherited
# facts have to be restated by every sprint after closure — dropping one is
# precisely the quiet contradiction LegacyRmeProgramClosureContractTest exists
# to catch.
inherits_closure: LEGACY-RME-PROGRAM-CLOSURE-1
inherits_hold: FIX-CLINIC-OPS-BRANCH-CONTEXT-WA-1/FIX-02
inherits_phase: feature-doctor-trusted-android-device-lock-1-phase-3-5-readiness-go
inherits_parent_go: doctor-pwa-webauthn-1-go
inherits_capability_go: doctor-pwa-multi-doctor-pilot-1-capability-go
inherits_pilot_go: doctor-pwa-multi-doctor-pilot-1-go
