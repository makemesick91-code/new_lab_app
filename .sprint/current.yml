# Active sprint manifest — POST-RME-ODONTOGRAM-STABILIZATION-1
#
# Validated by: php artisan sprint:manifest-check

id: POST-RME-ODONTOGRAM-STABILIZATION-1
type: RUNTIME_FIX
module: Odontogram
title: "Odontogram view creates no clinical row, repository SQLite artifact hygiene, RM number + registration date on the clinical calendar"
base_branch: feature/sprint-26-phase-26-8-stabilization-closure-go-watch-no-go-report

runtime_change: true             # odontogram read/write split, new store route + policy ability, ClinicalClock in RM composition
schema_change: false             # no migration, no table, no column
frontend_change: true            # resources/views/rme/visits/odontogram/show.blade.php — form action, print + finalize guards
security_impact: true            # new write-level policy ability; consent asserted BEFORE the insert; read-only operator no longer writes
branch_isolation_impact: false   # no BranchContext / branch-scoped query change; existing scopes re-pinned by tests
ledger_impact: false             # no inventory ledger / billing / payment behaviour change

deploy_required: true            # runtime fixes must reach production to be verified in realtime
browser_required: false          # server-rendered Blade; no JS behaviour change (Alpine payload untouched)

go_tag: post-rme-odontogram-stabilization-1-go

test_profiles:
  - rme
  - security

# Rule 107 is still ACTIVE, so the Full Suite is deferred, not passed.
full_suite_status: DEFERRED_BY_GLOBAL_TEMPORARY_POLICY   # FULL_SUITE_EXECUTION_COUNT=0

# Carried forward, unchanged by this sprint.
inherits_closure: LEGACY-RME-PROGRAM-CLOSURE-1
inherits_hold: FIX-CLINIC-OPS-BRANCH-CONTEXT-WA-1/FIX-02
