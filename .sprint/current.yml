# Active sprint manifest — LEGACY-RME-PDF-ROLL-2
# Validated by: php artisan sprint:manifest-check

id: LEGACY-RME-PDF-ROLL-2
type: MODULE_SPRINT
module: LegacyRme
title: "Controlled pilot enablement and operational readiness — a read-only, fail-closed gate that decides whether this deployment may switch the legacy RME archive ON, and proves it can be switched back OFF"
base_branch: feature/sprint-26-phase-26-8-stabilization-closure-go-watch-no-go-report

runtime_change: true            # new readiness service and artisan gate command
schema_change: false            # no migration; ROLL-2 adds no table or column
frontend_change: false          # no Blade, JS, CSS or Vite entry change
security_impact: true           # governs enablement of a clinical feature and its rollback path
branch_isolation_impact: false  # the pilot branch is validated, not resolved from a request
ledger_impact: false            # no inventory ledger / billing / payment behaviour change

deploy_required: true           # the gate must be runnable on the VPS before enablement
browser_required: false         # console gate; verified by test and by CLI on the host

test_profiles:
  - rme
  - security

go_tag: legacy-rme-pdf-roll-2-controlled-pilot-enablement-go