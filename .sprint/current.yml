# DEVFLOW-1 — Sprint Manifest
# Validated by: php artisan sprint:manifest-check

id: REVISION-SUNU-BRANCH-CODE-SUN4-TO-SPN4-1
type: MIGRATION_HEAVY
module: Branch
title: "Cabang Sunu's canonical branch code becomes SPN4; SUN4 is a deprecated historical alias — plus repair of the half-applied rename found in production"
base_branch: feature/sprint-26-phase-26-8-stabilization-closure-go-watch-no-go-report

# Impact flags — MUST match reality; CI + sprint:manifest-check enforce them.
runtime_change: true          # BranchCodeAlias registry entry + RmeBranchSeeder canonical key and restore branch
schema_change: true           # data migration: branch code, patient Nomor RM branch segment, live rollout rows
frontend_change: false        # no Blade/JS/CSS/Vite change; branch codes are rendered from data
security_impact: true         # branch identity is the isolation boundary, and RM-derived branch resolution feeds rollout admission
branch_isolation_impact: true # the branch code itself is revised; branch id and BranchContext authority are unchanged
ledger_impact: false          # no invoice/payment/inventory/earning path touched

deploy_required: true
browser_required: true

test_profiles: []

go_tag: revision-sunu-branch-code-sun4-to-spn4-1-go

# Carried forward from the preceding sprint's manifest. `.sprint/current.yml` is
# a rolling file, so these inherited facts have to be restated by every sprint
# after closure — dropping one is precisely the quiet contradiction
# LegacyRmeProgramClosureContractTest exists to catch.
inherits_closure: LEGACY-RME-PROGRAM-CLOSURE-1
inherits_hold: FIX-CLINIC-OPS-BRANCH-CONTEXT-WA-1/FIX-02
