# Active sprint manifest — LEGACY-RME-PDF-HISTORY-1B
# Validated by: php artisan sprint:manifest-check

id: LEGACY-RME-PDF-HISTORY-1B
type: MODULE_SPRINT
module: LegacyRme
title: "Doctor clinical branch context & production read UAT — a doctor's legacy branch scope is their canonical assigned practice set (mst_doctor_branches ∩ active RME-enabled), not an ephemeral BranchContext pin, so a treating doctor can read their patient's published archive while the treating-relationship and branch-isolation gates both stay enforced"
base_branch: feature/sprint-26-phase-26-8-stabilization-closure-go-watch-no-go-report

runtime_change: true            # new doctor branch resolver + legacy branch-scope wiring
schema_change: false            # no migration; mst_doctor_branches already exists and is populated
frontend_change: false          # no Blade markup, no JS/CSS, no Vite entry
security_impact: true           # decides which branches' clinical archive a doctor may read
branch_isolation_impact: true   # redefines the doctor branch scope; isolation is re-asserted by tests
ledger_impact: false            # no inventory ledger / billing / payment behaviour change

deploy_required: true           # the read-path change ships to the VPS pilot
browser_required: false         # verified by feature tests through the real HTTP routes, plus production UAT

test_profiles:
  - rme
  - security

go_tag: legacy-rme-pdf-history-1b-doctor-clinical-branch-context-production-read-uat-go
