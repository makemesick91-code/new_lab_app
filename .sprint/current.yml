# Active sprint manifest — PWA-FOUNDATION-1
# Validated by: php artisan sprint:manifest-check

id: PWA-FOUNDATION-1
type: MODULE_SPRINT
module: Pwa
title: "Installable PWA foundation — web app manifest, icons, standalone display, deny-by-default service worker cache, safe offline fallback (security-neutral: no authentication, device-authorization or RBAC change)"
base_branch: feature/sprint-26-phase-26-8-stabilization-closure-go-watch-no-go-report

runtime_change: false           # no controller, service, repository, model, command or policy
schema_change: false            # no migration, no table, no column
frontend_change: true           # Blade head partial, Vite entry registration, static public/ assets
security_impact: true           # a new client-side cache surface; deny-by-default policy + route sweep
branch_isolation_impact: false  # no BranchContext / branch-scoped query change
ledger_impact: false            # no inventory ledger / billing / payment behaviour change

deploy_required: true           # static assets + layout metadata must reach production
browser_required: true          # installability is only observable in a browser

test_profiles:
  - security
  - ui

# Carried forward. `.sprint/current.yml` is a ROLLING file, so these inherited
# facts have to be restated by every sprint after closure — dropping one is
# precisely the quiet contradiction LegacyRmeProgramClosureContractTest exists
# to catch. This sprint dropped the whole block on its first candidate and the
# gate caught it, exactly as the previous sprint recorded happening to it.
# Restating all of them, rather than only the asserted one, is the point.
inherits_closure: LEGACY-RME-PROGRAM-CLOSURE-1
inherits_hold: FIX-CLINIC-OPS-BRANCH-CONTEXT-WA-1/FIX-02
inherits_phase: feature-doctor-trusted-android-device-lock-1-phase-3-5-readiness-go
inherits_preflight: evidence-phase4a-real-device-keyinfo-preflight-1-go
inherits_custody: production-android-signing-custody-readiness-1-go
inherits_vault: revision-production-signing-custodian1-encrypted-vault-1-go
inherits_key: production-android-signing-key-provisioning-1-go
inherits_pin: production-android-signing-certificate-pin-1-go
inherits_release_artifact: production-android-release-apk-evidence-1-go
inherits_pilot_preparation: phase4a-doctor-android-pilot-preparation-1-go

# The Phase 4A doctor-device pilot is ARMED in production and its sprint is
# frozen mid-flight (PR #392 open, unmerged, no GO tag). PWA-FOUNDATION-1 is
# deliberately built on 1555ad5b — the exact commit production is running — and
# touches no authentication, device-authorization or enforcement surface, so the
# pilot's state is unaffected. The enforcement scope is captured before and
# after deploy and must be identical: covered=[18], denied 1, allowed 14,
# GLOBAL_ENFORCEMENT_ACTIVE=false.
inherits_armed_pilot: PHASE4A-DOCTOR-ANDROID-PILOT-ACTIVATION-1 (frozen, armed, unmerged)

# No go_tag until the sprint closes: a GO tag requires CI green, a merge, a VPS
# deploy from the runner ON the server, and production verification of the
# manifest, icons, service worker and offline shell over HTTPS.
