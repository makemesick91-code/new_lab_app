# Active sprint manifest — LEGACY-RME-PDF-1D
# Validated by: php artisan sprint:manifest-check

id: LEGACY-RME-PDF-1D
type: MODULE_SPRINT
module: LegacyRme
title: "VOID and clinical read/print completion — terminal reasoned VOID that retracts without erasing, a doctor-facing read-only archive viewer under a new least-privilege permission, and authorized private print/PDF export"
base_branch: feature/sprint-26-phase-26-8-stabilization-closure-go-watch-no-go-report

runtime_change: true            # void service, doctor viewer, print/export routes and controller actions
schema_change: false            # voided_by / voided_at / void_reason already exist since 1A — no migration
frontend_change: false          # Blade only; no JS/CSS/Vite entry change
security_impact: true           # new permission, policy change, doctor access boundary, private file exposure surface
branch_isolation_impact: true   # doctor viewer resolves branch scope server-side via LegacyRmeWorkspaceScope
ledger_impact: false            # no inventory ledger / billing / payment behaviour change

deploy_required: true           # runtime + permission seeding must reach the VPS
browser_required: false         # print is a Blade view; verified by test, not by an interactive browser

test_profiles:
  - rme
  - security

go_tag: legacy-rme-pdf-1d-void-clinical-read-print-go