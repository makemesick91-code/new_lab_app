# DEVFLOW-1 — Sprint Manifest
# Validated by: php artisan sprint:manifest-check

id: FEATURE-DOCTOR-ACCOUNT-PERFORMANCE-INCOME-LINKAGE-1
type: MODULE_SPRINT
module: Doctor
title: "Doctor account identity link — Master Data management UI for the existing mst_doctors.user_id relation, one canonical identity resolver, and removal of the email-matching fallback"
base_branch: feature/sprint-26-phase-26-8-stabilization-closure-go-watch-no-go-report

# Impact flags — MUST match reality; CI + sprint:manifest-check enforce them.
runtime_change: true          # new routes/controller/service/resolver; DoctorUserResolver behaviour hardened
schema_change: false          # mst_doctors.user_id already exists (nullable, unique FK) — no migration
frontend_change: true         # new Blade management page + sidebar entry
security_impact: true         # new permission, new policy abilities, identity-resolution authority change
branch_isolation_impact: false # doctor identity is not branch-scoped; existing branch rules untouched
ledger_impact: false          # no invoice/payment/inventory/earning calculation touched

deploy_required: true
browser_required: true

test_profiles: []

go_tag: feature-doctor-account-performance-income-linkage-1-go

# Carried forward from the preceding sprint's manifest. `.sprint/current.yml` is
# a rolling file, so these inherited facts have to be restated by every sprint
# after closure — dropping one is precisely the quiet contradiction
# LegacyRmeProgramClosureContractTest exists to catch.
inherits_closure: LEGACY-RME-PROGRAM-CLOSURE-1
inherits_hold: FIX-CLINIC-OPS-BRANCH-CONTEXT-WA-1/FIX-02
