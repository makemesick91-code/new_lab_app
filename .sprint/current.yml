# Active sprint manifest — LEGACY-RME-PDF-1A
# Validated by: php artisan sprint:manifest-check

id: LEGACY-RME-PDF-1A
type: MODULE_SPRINT
module: LegacyRme
title: "Legacy RME PDF archive foundation — schema, permissions, policies and date rules (feature flag OFF; no upload/publish runtime, no HTTP surface)"
base_branch: feature/sprint-26-phase-26-8-stabilization-closure-go-watch-no-go-report

runtime_change: true            # new bounded context: models, repositories, date resolver + rule service, audit facade, feature guard
schema_change: true             # 4 additive migrations (2 staging + 2 archive tables); no native RME table altered
frontend_change: false          # foundation only — no route, no controller, no Blade view in 1A
security_impact: true           # 5 new permissions, 2 policies, fail-closed branch scope, PII-free audit payload policy
branch_isolation_impact: true   # legacy rows scoped server-side from BranchContext / the RME-enabled branch set; origin branch is provenance, never authorization
ledger_impact: false            # no inventory ledger / billing / payment behaviour change

deploy_required: true
browser_required: false         # no UI surface ships in this sprint

test_profiles:
  - rme
  - access_control
  - security

go_tag: legacy-rme-pdf-1a-schema-permission-date-rules-go
