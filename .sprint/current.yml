### Validated by: php artisan sprint:manifest-check

id: FIX-CLINIC-OPS-BRANCH-CONTEXT-WA-1
type: FOUNDATION_SPRINT
module: ClinicVisit
# Nine integrated fixes spanning ClinicVisit, RmeOnlineContext, RmeInvoice and
# Prescription, introducing the shared RmeWorkingBranchScope foundation the
# clinic and cashier surfaces all consume. FOUNDATION_SPRINT is the accurate
# classification for cross-module shared-foundation work, and it is the STRICTER
# choice: audit level 3 and full_foundation_audit, versus MODULE_SPRINT's 2.
title: "Clinic and cashier workspaces pinned to one working branch, branch document identity, WhatsApp Business prescription delivery, and Super Admin-only SATUSEHAT"
base_branch: feature/sprint-26-phase-26-8-stabilization-closure-go-watch-no-go-report

runtime_change: true            # branch scoping, visit workflow, prescription delivery
schema_change: true             # one additive table for WhatsApp delivery records
frontend_change: true           # Blade only (no JS/CSS/Vite change)
security_impact: true           # branch isolation, a new capability, a Super Admin gate
branch_isolation_impact: true   # the core of the sprint
ledger_impact: false            # no inventory ledger; payment branch scope tightened, amounts untouched

deploy_required: true
go_tag: fix-clinic-ops-branch-context-wa-1-go
browser_required: false         # every surface is proven by HTTP feature tests

# Inherited programme state (LegacyRmeProgramClosureContractTest): this sprint
# runs after LEGACY-RME-PROGRAM-CLOSURE-1 and inherits that closure unchanged.
# It touches no Legacy RME code, capability, admission, wave or batch.
# ENGINEERING_ROLLOUT_MODE stays CLOSED and STEADY_STATE_OPERATIONS stays
# AUTHORITATIVE; the Legacy RME resting state (capability OFF, admission EMPTY,
# no active batch) is untouched.
inherits_closure: LEGACY-RME-PROGRAM-CLOSURE-1

test_profiles:
  - rme
  - security
  - permission
