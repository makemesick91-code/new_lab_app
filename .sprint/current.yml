# Active sprint manifest — LEGACY-RME-SOURCE-RM-BINDING-1
# Validated by: php artisan sprint:manifest-check

id: LEGACY-RME-SOURCE-RM-BINDING-1
type: MODULE_SPRINT
module: LegacyRme
title: "Source RM capture, patient-binding enforcement & wrong-patient prevention — the RM printed on a legacy document becomes first-class structured evidence, is resolved exactly, and must name the selected patient before anything is staged, queued, reviewed or published"
base_branch: feature/sprint-26-phase-26-8-stabilization-closure-go-watch-no-go-report

runtime_change: true            # new binding guard on the legacy import write path + review/publish revalidation
schema_change: true             # additive nullable source-RM columns on staging + published legacy tables
frontend_change: true           # Blade only — source RM input + attestation on the legacy import form
security_impact: true           # wrong-patient binding prevention, exact-only identity resolution, branch authority

deploy_required: true           # migration must run on the VPS
browser_required: false         # server-side enforcement; UI is a form field, no interactive flow to drive

test_profiles:
  - legacy_rme
  - security
  - permission

# Declared, not yet created. The tag is only cut after the migration is applied
# on the VPS and the deployed diagnostic proves the binding gate refuses a wrong
# patient in production.
go_tag: legacy-rme-source-rm-binding-1-source-rm-capture-patient-binding-enforcement-go
