# DEVFLOW-1 — Sprint Manifest
# Validated by: php artisan sprint:manifest-check

id: REVISION-SUNU-ADD-ROOM-A-B-1
type: MODULE_SPRINT
module: ClinicRoom
title: "Cabang Sunu (SPN4) gets two active operational rooms, Ruangan A and Ruangan B, provisioned idempotently onto the existing branch identity"
base_branch: feature/sprint-26-phase-26-8-stabilization-closure-go-watch-no-go-report

# Impact flags — MUST match reality; CI + sprint:manifest-check enforce them.
runtime_change: true          # new RmeBranchRoomSeeder + DatabaseSeeder registration; no controller/service/route touched
schema_change: false          # no migration: mst_clinic_rooms already exists, this is master data only
frontend_change: false        # no Blade/JS/CSS/Vite change; room selectors already read the master data dynamically
security_impact: true         # rooms are branch-scoped, and the cross-branch room-assignment boundary is re-pinned
branch_isolation_impact: true # rooms hang off branch_id; provisioning resolves the branch by code, never by request or display name
ledger_impact: false          # no invoice/payment/inventory/earning path touched

deploy_required: true
browser_required: false       # no UI change; the room selectors are unmodified and read master data dynamically

test_profiles: []

go_tag: revision-sunu-add-room-a-b-1-go

# Carried forward from the preceding sprint's manifest. `.sprint/current.yml` is
# a rolling file, so these inherited facts have to be restated by every sprint
# after closure — dropping one is precisely the quiet contradiction
# LegacyRmeProgramClosureContractTest exists to catch.
inherits_closure: LEGACY-RME-PROGRAM-CLOSURE-1
inherits_hold: FIX-CLINIC-OPS-BRANCH-CONTEXT-WA-1/FIX-02
