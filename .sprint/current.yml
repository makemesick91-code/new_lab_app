# Active sprint manifest — LEGACY-RME-PDF-ROLL-4-WAVE-1
# Validated by: php artisan sprint:manifest-check

id: LEGACY-RME-PDF-ROLL-4-WAVE-1
type: DOCS_ONLY
module: LegacyRme
title: "Controlled production migration Wave-1 (LDK2) — WATCH: no genuine candidate document and no separation-of-duties operator; production left at the safe resting state"
base_branch: feature/sprint-26-phase-26-8-stabilization-closure-go-watch-no-go-report

runtime_change: false           # no app code touched; ROLL-4 control plane used as shipped
schema_change: false            # no migration, no table, no column
frontend_change: false          # no Blade view, no JS/CSS, no Vite entry
security_impact: false          # no permission granted, no role changed, no policy touched
branch_isolation_impact: false  # no BranchContext / branch-scoped query change
ledger_impact: false            # no inventory ledger / billing / payment behaviour change

deploy_required: false          # documentation and governance evidence only
browser_required: false         # no UI surface

test_profiles:
  - docs

# Outcome: WATCH. No wave registered, no capability enabled, no import accepted,
# no GO tag. Production verified unchanged: capability OFF, admission EMPTY,
# approval EMPTY, active wave NONE, zero operational wave rows.
#
# Blockers recorded in
# docs/sprints/legacy-rme-pdf-roll-4-wave-1-controlled-production-migration.md
