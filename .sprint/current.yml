# MONITORING-LOGS-WATCH-ROOT-CAUSE-1 — Sprint Manifest
#
# Root-causes the production `logs = WATCH`. The WATCH itself was CORRECT: five real
# ERROR events sat inside the 24h lookback window. Nothing was suppressed to make it
# green. The audit that proved the WATCH correct also found the logs section could
# report OK having observed nothing at all, and that is what this sprint fixes.

id: MONITORING-LOGS-WATCH-ROOT-CAUSE-1
type: RUNTIME_FIX
module: Monitoring
title: "Production logs=WATCH root-caused as correct; the logs section can no longer report OK without evidence"
base_branch: feature/sprint-26-phase-26-8-stabilization-closure-go-watch-no-go-report

# Impact flags — MUST match reality.
runtime_change: true          # analyzer/classifier/service fail closed instead of fail open
schema_change: false
frontend_change: false
security_impact: false        # no policy/middleware/permission/route change
branch_isolation_impact: false
ledger_impact: false

deploy_required: true
browser_required: false

test_profiles: []

go_tag: monitoring-logs-watch-root-cause-1-go

# Rule 107 is still ACTIVE, so the Full Suite is deferred, not passed.
full_suite_status: DEFERRED_BY_GLOBAL_TEMPORARY_POLICY   # FULL_SUITE_EXECUTION_COUNT=0

# Carried forward, unchanged by this sprint. The Legacy RME programme stays closed
# and at rest (capability OFF, admission empty, no batch opened), and the
# prescription WhatsApp provider stays on hold pending its external dependency.
inherits_closure: LEGACY-RME-PROGRAM-CLOSURE-1
inherits_hold: FIX-CLINIC-OPS-BRANCH-CONTEXT-WA-1/FIX-02
