# Active sprint manifest — FIX-LOGIN-REDIRECT-RUNTIME-PERMISSIONS
# Validated by: php artisan sprint:manifest-check

id: FIX-LOGIN-REDIRECT-RUNTIME-PERMISSIONS
type: RUNTIME_FIX
module: Auth
title: "Login recovery: role-aware safe post-auth redirect + deploy runtime-permission hardening"
base_branch: feature/sprint-26-phase-26-8-stabilization-closure-go-watch-no-go-report

runtime_change: true            # centralized post-auth redirect resolver + auth completion controllers + smoke command
schema_change: false            # no migration
frontend_change: false          # no JS/CSS/Vite/tailwind/build change
security_impact: true           # post-auth authorization behavior (intended-URL sanitization; Admin Lab stays Lab-only)
branch_isolation_impact: false  # no branch-scoping change
ledger_impact: false            # inventory ledger untouched

deploy_required: true
browser_required: true          # authenticated Admin Lab / Super Admin landing smoke

test_profiles:
  - auth
  - access_control
  - deployment

go_tag: fix-login-redirect-runtime-permissions-go
