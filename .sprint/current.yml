# Active sprint manifest — LEGACY-RME-PDF-HISTORY-1
# Validated by: php artisan sprint:manifest-check

id: LEGACY-RME-PDF-HISTORY-1
type: MODULE_SPRINT
module: LegacyRme
title: "Patient-centric clinical history integration — the RME workspace shows the current RME, the earlier native RME and the PUBLISHED legacy archive as one read-only history, without converting a legacy record into a native one"
base_branch: feature/sprint-26-phase-26-8-stabilization-closure-go-watch-no-go-report

runtime_change: true            # history read model + the RME workspace controller
schema_change: false            # no migration; every column already existed since 1A
frontend_change: true           # new read-only Blade partial in the RME workspace
security_impact: true           # governs which clinical archive a doctor may see
branch_isolation_impact: true   # legacy branch scope is re-checked on the read path
ledger_impact: false            # no inventory ledger / billing / payment behaviour change

deploy_required: true           # the workspace change ships to the VPS pilot
browser_required: false         # verified by feature tests rendering the workspace

test_profiles:
  - rme
  - security

go_tag: legacy-rme-pdf-history-1-patient-centric-clinical-history-integration-go
