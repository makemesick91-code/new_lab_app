# Active sprint manifest — LEGACY-ODONTOGRAM-NATIVE-REFERENCE-CUTOFF-1
#
# Validated by: php artisan sprint:manifest-check

id: LEGACY-ODONTOGRAM-NATIVE-REFERENCE-CUTOFF-1
type: RUNTIME_FIX
module: LegacyOdontogram
title: "An empty native odontogram placeholder is not clinical evidence for the legacy odontogram native-reference cutoff"
base_branch: feature/sprint-26-phase-26-8-stabilization-closure-go-watch-no-go-report

runtime_change: true             # the native-reference read boundary now requires meaningful clinical content
schema_change: false             # no migration, no table, no column, no index
frontend_change: false           # no Blade/JS change — the rule messages and codes are unchanged
security_impact: true            # changes which documents may enter a patient's clinical archive (eligibility boundary)
branch_isolation_impact: false   # the resolver stays deliberately un-branch-scoped; no BranchContext change
ledger_impact: false             # no inventory ledger / billing / payment behaviour change

deploy_required: true            # the corrected cutoff must reach production to be verified in realtime
browser_required: false          # server-side domain rule only

go_tag: legacy-odontogram-native-reference-cutoff-1-go

test_profiles:
  - rme
  - security

# Rule 107 is still ACTIVE, so the Full Suite is deferred, not passed.
full_suite_status: DEFERRED_BY_GLOBAL_TEMPORARY_POLICY   # FULL_SUITE_EXECUTION_COUNT=0

# Carried forward, unchanged by this sprint.
inherits_closure: LEGACY-RME-PROGRAM-CLOSURE-1
inherits_hold: FIX-CLINIC-OPS-BRANCH-CONTEXT-WA-1/FIX-02
