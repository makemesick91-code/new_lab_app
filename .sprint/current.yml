# Active sprint manifest — LEGACY-RME-SOD-1
# Validated by: php artisan sprint:manifest-check

id: LEGACY-RME-SOD-1
type: MODULE_SPRINT
module: LegacyRme
title: "Mandatory separate-publisher enforcement & production activation — one canonical server-side separation-of-duties gate, re-asserted under the publish/review row lock, fail-closed by default, activated in production"
base_branch: feature/sprint-26-phase-26-8-stabilization-closure-go-watch-no-go-report

runtime_change: true            # lifecycle + publish service gate, config default flip, controller/view surface
schema_change: false            # no migration, no table, no column
frontend_change: false          # Blade markup only — no JS, CSS, Vite entry or build file
security_impact: true           # separation of duties is an authorization-adjacent business invariant
branch_isolation_impact: false  # branch scope resolution untouched
ledger_impact: false            # no inventory ledger / billing / payment behaviour change

deploy_required: true           # production must be redeployed and the flag activated
browser_required: false         # UI change is a permission-shaped notice, covered by feature tests

test_profiles:
  - legacy_rme
  - security

go_tag: legacy-rme-sod-1-mandatory-separate-publisher-enforcement-production-activation-go
