# Active sprint manifest — LEGACY-RME-STEADY-STATE-OPS-1
# Validated by: php artisan sprint:manifest-check

id: LEGACY-RME-STEADY-STATE-OPS-1
type: MODULE_SPRINT
module: LegacyRme
title: "Legacy RME steady-state operations — routine batch operating model, consolidated pre-flight readiness (backup freshness, resting state, branch matrix), SOP and multi-branch classification"
base_branch: feature/sprint-26-phase-26-8-stabilization-closure-go-watch-no-go-report

runtime_change: true            # new read-only aggregator service + one read-only artisan command
schema_change: false            # no migration, no table, no column
frontend_change: false          # no Blade view, no JS/CSS, no Vite entry — CLI + docs only
security_impact: true           # reads SOD posture and admission/approval state; adds a backup gate. Widens nothing.
branch_isolation_impact: false  # no BranchContext change; branch matrix is read-only over RME-enabled branches
ledger_impact: false            # no inventory ledger / billing / payment behaviour change

deploy_required: true           # the command and config must exist on the VPS for operators to use them
browser_required: false         # no UI surface added

test_profiles:
  - legacy_rme
  - security

go_tag: legacy-rme-steady-state-ops-1-operationalization-multi-branch-routine-migration-go
