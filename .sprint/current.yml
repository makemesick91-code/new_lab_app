# Active sprint manifest — FEATURE-DOCTOR-TRUSTED-ANDROID-DEVICE-LOCK-1 Phase 3
# Validated by: php artisan sprint:manifest-check

id: FEATURE-DOCTOR-TRUSTED-ANDROID-DEVICE-LOCK-1-PHASE-3
type: MODULE_SPRINT
module: DoctorDevice
title: "Android Clinic App + cryptographic device enrolment capability (Keystore identity, challenge/response, secure WebView, kiosk; device login enforcement stays OFF)"
base_branch: feature/sprint-26-phase-26-8-stabilization-closure-go-watch-no-go-report

runtime_change: true            # enrolment/proof services + stateless device API routes
schema_change: true             # additive: enrolment + challenge tables, enrolment columns on the registry
frontend_change: true           # pending-enrolment approval UI on the existing Device Dokter screen
security_impact: true           # cryptographic device identity, challenge/response, WebView origin + TLS policy
branch_isolation_impact: false  # no BranchContext / branch-scoped query change
ledger_impact: false            # no inventory ledger / billing / payment behaviour change

deploy_required: true           # additive migrations + device API on the VPS pilot
browser_required: false         # server-rendered Blade; Android validated on emulator

test_profiles:
  - security
  - rme

# Carried forward from the preceding sprint's manifest. `.sprint/current.yml` is
# a rolling file, so these inherited facts have to be restated by every sprint
# after closure — dropping one is precisely the quiet contradiction
# LegacyRmeProgramClosureContractTest exists to catch.
inherits_closure: LEGACY-RME-PROGRAM-CLOSURE-1
inherits_hold: FIX-CLINIC-OPS-BRANCH-CONTEXT-WA-1/FIX-02

# Phase 3 is CAPABILITY ONLY. The full feature stays NO-GO: device enforcement is
# not active, real-device validation has NOT been performed, production signing
# governance is UNRESOLVED, and the full-feature GO tag
# `feature-doctor-trusted-android-device-lock-1-go` must NOT be created.
inherits_phase: feature-doctor-trusted-android-device-lock-1-phase-2-go
