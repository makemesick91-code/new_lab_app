### Active sprint manifest — DOCTOR-PWA-WEBAUTHN-CREDENTIAL-REVOKE-CONTAINMENT-1
### Validated by: php artisan sprint:manifest-check

id: DOCTOR-PWA-WEBAUTHN-CREDENTIAL-REVOKE-CONTAINMENT-1
type: SECURITY_FIX
module: DoctorDevice
title: "Revoking one WebAuthn credential is proven on the live pilot tablet to contain exactly the session it authenticated — a pre-existing Android session on the same dual-proof device survives, the device and authorization are untouched, and a fresh device-bound credential recovers the doctor"
base_branch: feature/sprint-26-phase-26-8-stabilization-closure-go-watch-no-go-report

runtime_change: false           # the runtime is byte-identical to the base authority.
                                # PROOF-BINDING-1 already re-verifies a WebAuthn session
                                # against its exact credential, and the source audit found
                                # no defect to manufacture. What was missing was coverage
                                # of the blast radius, not the containment itself.
schema_change: false            # no migration; no column; no data touched
frontend_change: false          # no JS, no CSS, no Blade
security_impact: true           # it is an authentication containment path, gated as one
branch_isolation_impact: false  # branch stays a property of the device row
ledger_impact: false            # no inventory ledger / billing / payment behaviour

deploy_required: true           # NOT for behaviour — production already carries the
                                # mechanism. The deploy aligns production HEAD with the
                                # merge SHA so the live revocation evidence and the GO
                                # tag describe the same authority.
browser_required: true          # a credential is revoked by an operator in one browser
                                # while a doctor session stays open in another, on the
                                # physical SM-X236B, alongside a real Clinic App session.
                                # Nothing in the suite can hold three live sessions on
                                # real hardware.

test_profiles:
  - security

# Carried forward. `.sprint/current.yml` is a ROLLING file, so these inherited
# facts have to be restated by every sprint after closure — dropping one is
# precisely the quiet contradiction LegacyRmeProgramClosureContractTest exists
# to catch.
inherits_closure: LEGACY-RME-PROGRAM-CLOSURE-1
inherits_hold: FIX-CLINIC-OPS-BRANCH-CONTEXT-WA-1/FIX-02
inherits_phase: feature-doctor-trusted-android-device-lock-1-phase-3-5-readiness-go
