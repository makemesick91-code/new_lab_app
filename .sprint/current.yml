### Validated by: php artisan sprint:manifest-check

id: FIX-RME-EXAM-CONSENT-ODONTOGRAM-HISTORY-3
type: FOUNDATION_SPRINT
module: ClinicVisit
# Four integrated fixes that together re-time the whole RME clinical workflow.
# FIX-01 separates clinical-document finalization from examination completion.
# FIX-02 moves the consent gate from payment to RME authoring, which is a
# cross-module security foundation (Consent, MedicalRecord, ClinicVisit).
# FIX-03 removes consent from cashier/payment eligibility entirely.
# FIX-04 adds a read-only patient odontogram history plus a new immutable
# Legacy Odontogram archive. FOUNDATION_SPRINT is the accurate classification
# for cross-module foundation work and is the STRICTER choice: audit level 3 and
# full_foundation_audit, versus MODULE_SPRINT's 2.
title: "Explicit doctor examination completion, consent as the RME authoring gate, cashier/payment consent independence, and patient odontogram history with a Legacy Odontogram archive"
base_branch: feature/sprint-26-phase-26-8-stabilization-closure-go-watch-no-go-report

runtime_change: true            # workflow transitions, consent gate, payment eligibility
schema_change: true             # additive tables for the Legacy Odontogram archive
frontend_change: true           # Blade only (no JS/CSS/Vite change)
security_impact: true           # a trust boundary MOVES: consent now gates clinical writes
branch_isolation_impact: true   # patient odontogram history and the legacy archive are branch scoped
ledger_impact: false            # no inventory ledger; payment amounts untouched

deploy_required: true
go_tag: fix-rme-exam-consent-odontogram-history-3-go
browser_required: false         # every surface is proven by HTTP feature tests

# GLOBAL TEMPORARY FULL-SUITE POLICY is ACTIVE for this sprint. The Full Suite is
# NOT executed here; it is deferred, not passed. FULL_SUITE_EXECUTION_COUNT=0.
full_suite_status: DEFERRED_BY_GLOBAL_TEMPORARY_POLICY

# Inherited programme state (LegacyRmeProgramClosureContractTest): this sprint
# inherits LEGACY-RME-PROGRAM-CLOSURE-1 unchanged. It adds a SEPARATE Legacy
# Odontogram archive with its own flag, tables and permissions, and deliberately
# does NOT open a Legacy RME migration batch, wave, quota or admission. The Legacy
# RME resting state (capability OFF, admission EMPTY, no active batch) is untouched.
inherits_closure: LEGACY-RME-PROGRAM-CLOSURE-1

# FIX-CLINIC-OPS-BRANCH-CONTEXT-WA-1 / FIX-02 (WhatsApp prescription delivery)
# stays on HOLD / BLOCKED_EXTERNAL_DEPENDENCY. This sprint does not activate it,
# does not touch the gateway, the configuration or the credentials.
inherits_hold: FIX-CLINIC-OPS-BRANCH-CONTEXT-WA-1/FIX-02

test_profiles:
  - rme
  - security
  - permission
