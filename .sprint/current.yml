# Active sprint manifest — BUGFIX-DOCTOR-PWA-WEBAUTHN-VERIFY-BUTTON-1
# Validated by: php artisan sprint:manifest-check

id: BUGFIX-DOCTOR-PWA-WEBAUTHN-VERIFY-BUTTON-1
type: RUNTIME_FIX
module: DoctorDevice
title: "The doctor device step shipped a verify button with no handler — layouts/guest.blade.php had no @stack('scripts'), so the page's pushed assertion wiring was discarded at render time on every browser"
base_branch: feature/sprint-26-phase-26-8-stabilization-closure-go-watch-no-go-report

runtime_change: true            # the rendered response gains the wiring it always intended to ship
schema_change: false            # no migration; no column; no data touched
frontend_change: true           # layouts/guest.blade.php + delivered inline script
security_impact: true           # it is an authentication path, so it is gated as one
branch_isolation_impact: false  # branch stays a property of the device row
ledger_impact: false            # no inventory ledger / billing / payment behaviour

deploy_required: true           # a render-time defect is only fixed once production renders it
browser_required: true          # the defect was invisible to the server suite and only a real
                                # SM-X236B assertion can prove the button now reaches the
                                # Android authenticator

test_profiles:
  - security

# Carried forward. `.sprint/current.yml` is a ROLLING file, so these inherited
# facts have to be restated by every sprint after closure — dropping one is
# precisely the quiet contradiction LegacyRmeProgramClosureContractTest exists
# to catch.
inherits_closure: LEGACY-RME-PROGRAM-CLOSURE-1
inherits_hold: FIX-CLINIC-OPS-BRANCH-CONTEXT-WA-1/FIX-02
inherits_phase: feature-doctor-trusted-android-device-lock-1-phase-3-5-readiness-go
