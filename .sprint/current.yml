### Active sprint manifest — DOCTOR-PWA-WEBAUTHN-PARENT-CLOSURE-1
### Validated by: php artisan sprint:manifest-check

id: DOCTOR-PWA-WEBAUTHN-PARENT-CLOSURE-1
type: SECURITY_FIX
module: DoctorDevice
title: "The parent DOCTOR-PWA-WEBAUTHN-1 programme closes against the configuration production actually runs: a WebAuthn login completed while the enforcement scope names one pilot doctor and the fleet-wide permit stays shut, plus the device-binding revalidation a mutation campaign proved nothing was holding"
base_branch: feature/sprint-26-phase-26-8-stabilization-closure-go-watch-no-go-report

runtime_change: false           # no application source changed. Every mutation applied
                                # during the sensitivity campaign was restored by copy and
                                # `git status` proves zero tracked modifications. The
                                # closure adds coverage and durable rules, not behaviour.
schema_change: false            # no migration; no column; no data touched
frontend_change: false          # no JS, no CSS, no Blade
security_impact: true           # it is the authentication admission path, gated as one
branch_isolation_impact: false  # branch stays a property of the device row
ledger_impact: false            # no inventory ledger / billing / payment behaviour

deploy_required: true           # NOT for behaviour — production already carries the
                                # mechanism. The deploy aligns production HEAD with the
                                # merge SHA so the final ceremony evidence and the parent
                                # GO tag describe the same authority.
browser_required: true          # the parent GO turns on a real physical gate: drg Karmila
                                # completing a WebAuthn assertion on the approved
                                # SM-X236B, in Chrome AND in the installed PWA. A platform
                                # authenticator, a user-verification prompt and an
                                # installed-app client context cannot be simulated by any
                                # suite, and a parent GO taken without them would be a
                                # claim rather than a proof.

test_profiles:
  - security

# Carried forward. `.sprint/current.yml` is a ROLLING file, so these inherited
# facts have to be restated by every sprint after closure — dropping one is
# precisely the quiet contradiction LegacyRmeProgramClosureContractTest exists
# to catch.
inherits_closure: LEGACY-RME-PROGRAM-CLOSURE-1
inherits_hold: FIX-CLINIC-OPS-BRANCH-CONTEXT-WA-1/FIX-02
inherits_phase: feature-doctor-trusted-android-device-lock-1-phase-3-5-readiness-go
