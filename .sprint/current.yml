# Active sprint manifest — LEGACY-RME-PDF-1B
# Validated by: php artisan sprint:manifest-check

id: LEGACY-RME-PDF-1B
type: MODULE_SPRINT
module: LegacyRme
title: "Legacy RME PDF archive — private upload runtime, PDF validation, queue processing, page rendering and thumbnails (feature flag OFF; no review/publish runtime)"
base_branch: feature/sprint-26-phase-26-8-stabilization-closure-go-watch-no-go-report

runtime_change: true            # upload service, queued rasterization job, private streaming, HTTP surface
schema_change: false            # 1A already shipped every column this sprint writes to
frontend_change: true           # 3 new Blade views + sidebar entry (Alpine only; no JS/CSS bundle source change)
security_impact: true           # private disk, policy abilities, IDOR boundaries, process execution, malware scan hook
branch_isolation_impact: true   # every staged row resolved through the repository with the server-resolved branch scope
ledger_impact: false            # no inventory ledger / billing / payment behaviour change

deploy_required: true
browser_required: false

test_profiles:
  - rme
  - access_control
  - security

go_tag: legacy-rme-pdf-1b-private-upload-rendering-go
