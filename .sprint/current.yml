# Active sprint manifest — DEVFLOW-FIX-BASE-REF-1
# Validated by: php artisan sprint:manifest-check

id: DEVFLOW-FIX-BASE-REF-1
type: RUNTIME_FIX
module: Devflow
title: "Canonical remote base resolution — exact-SHA comparison authority for every DEVFLOW governance consumer; stale/ahead/diverged local base refs and unrelated checkouts can no longer change a result; remote failure fails closed instead of falling back"
base_branch: feature/sprint-26-phase-26-8-stabilization-closure-go-watch-no-go-report

runtime_change: true            # new resolver + artisan command + classifier hardening — governance/CI tooling only, no product runtime path
schema_change: false            # no migration, no table, no column
frontend_change: false          # no Blade view, no JS/CSS, no Vite entry
security_impact: true           # git ref/option injection surface, fail-closed authority, wrong-checkout isolation
branch_isolation_impact: false  # no BranchContext / branch-scoped query change
ledger_impact: false            # no inventory ledger / billing / payment behaviour change

deploy_required: true           # governance tooling ships to the VPS checkout and is smoke-verified there
browser_required: false         # no UI surface

go_tag: devflow-fix-base-ref-1-canonical-remote-base-resolution-go

test_profiles:
  - cicd
  - security
