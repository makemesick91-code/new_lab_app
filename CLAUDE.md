## Foundation-First Sprint Lock

Status: **FOUNDATION-FIRST LOCK ACTIVE**. All non-foundation work is POST-FOUNDATION BACKLOG and blocked until FOUNDATION GO. Read `docs/architecture/foundation-first-sprint-lock-governance.md` before planning or implementing sprint work.

## RME Module (Sprint 20)

**Modules:** `ClinicVisit`, `MedicalRecord`, `Odontogram`, `RmeInvoice` under `app/Modules/`.

**Routes:** `rme.*` prefix in `routes/web.php`.

**Workflow:** Admin creates visit → Doctor odontogram + handwriting RME → finalize →
`cashier_pending` → Cashier invoice + full payment → `completed` → print receipt.

**Visit statuses:** `registered`, `waiting`, `in_progress`, `cashier_pending`, `completed`, `cancelled`.

**Invoice statuses:** `DRAFT`, `UNPAID`, `PAID`, `VOID`.

**Rules:**
- Handwriting RM is the **primary doctor-facing clinical input**; SOAP fields are optional legacy structured fields hidden from doctor UI.
- Handwriting PNG **mandatory** before RME finalization; immutable after finalize.
- Initial service is triage-only — **no billing**.
- Cashier billing requires finalized RME + `cashier_pending` visit (`manage_rme_billing`).
- **Full payment only** in pilot — partial/cicilan deferred.
- Print via browser `window.print()` — no server PDF.

**Permissions:** `view_clinic_visits`, `manage_clinic_visits`, `manage_rme_billing`.

**Pilot doc:** `docs/sprint_20_rme_limited_pilot_summary.md`

**Pilot master data import:** use `php artisan rme:import-pilot-backup` (see `docs/rme_pilot_backup_import_guide.md`). Never restore backup SQL directly over the Sprint 20 database.

**UI:** All RME views use TailAdmin components (`x-ui.card`, `x-ui.table`, `x-ui.badge`, `x-ui.button`, `x-settings-shell`). UI modernization merged into `feature/sprint-20-rme-core` (merge commit `8246008`, tag `sprint-20-rme-ui-modernization-merged`).

**Sprint 20 closure (2026-06-11):** Final branch `feature/sprint-20-rme-core`. Closure tag `sprint-20-rme-core-ui-complete`. Full suite: 1842 passed / 6290 assertions. RME suite: 283 passed / 718 assertions. Do NOT re-open SOAP in doctor UI — handwriting RM is the primary clinical input and SOAP is hidden by design. Full-payment-only rule remains in force (partial/cicilan deferred to Sprint 21). Closure report: `docs/sprint_20_final_closure_report.md`.

**Sprint 21 planning (2026-06-11):** Planning branch `feature/sprint-21-planning`. Theme: RME Advanced Workflow + Pilot Deployment. Planning doc: `docs/sprint_21_planning.md`. First implementation phase: Phase 21.1 RME → Lab integration architecture (design only), then Phase 21.2 lab order generation (tests-first). SOAP doctor UI remains hidden. Full-payment-only behavior is the baseline — partial/cicilan changes begin only when Phase 21.4 is explicitly approved. No feature code added in planning phase.

**Sprint 21 Phase 21.1 — RME → Lab architecture (2026-06-11):** Branch `feature/sprint-21-rme-lab-architecture`. Tag `sprint-21-phase-21-1-rme-lab-architecture`. Architecture doc: `docs/sprint_21_rme_lab_integration_architecture.md`. Design only — no code changed. Key decisions: (1) RME → Lab trigger is after `RmePaymentService::pay()` sets invoice to `PAID`. (2) Pilot strategy: create `LabCaseCandidate` staging record first (not direct LabOrder) — `LabOrderItem` uses `lab_service_id` but RME items use `treatment_id`; no mapping exists yet. (3) One candidate per eligible `trx_rme_invoice_items` row where `mst_treatments.requires_lab = true` (column already exists). (4) Idempotent via `UNIQUE(rme_invoice_item_id)` on `trx_lab_case_candidates`. (5) Payment commits first; candidate generation is post-commit (failure does not roll back payment). (6) `branch_id` copied from RME invoice; validated against `BranchContext::requireId()`. (7) Do NOT create `trx_payments` (lab billing) records from RME payment — `trx_rme_payments` only. Phase 21.2 unblocked after project owner approves this architecture.

**Sprint 21 Phase 21.2 — Lab Case Candidate generation (2026-06-11):** Branch `feature/sprint-21-lab-case-candidates`. Tag `sprint-21-phase-21-2-lab-case-candidates`. Implements the first functional RME → Lab integration layer. New table `trx_lab_case_candidates` (migration `2026_06_14_210001`). New model `App\Modules\LabOrder\Models\LabCaseCandidate` (status constants: `pending_review`, `converted_to_lab_order`, `rejected`, `cancelled`). New service `App\Modules\RmeInvoice\Services\RmeLabIntegrationService` — `generateForPaidInvoice()` is idempotent via `firstOrCreate(rme_invoice_item_id)`. Post-commit hook in `RmePaymentService::pay()` calls generation after payment transaction commits; generation failure logs warning and does NOT roll back payment. Branch isolation: service validates `invoice.branch_id === BranchContext::id()`. No real `LabOrder` created — candidates await Admin Lab review. 11 tests in `tests/Feature/RME/LabIntegrationTest.php`. Full suite: 1853 passed. Sprint 20 full-payment-only rule preserved.

## graphify

This project has a knowledge graph at graphify-out/ with god nodes, community structure, and cross-file relationships.

Rules:
- For codebase questions, first run `graphify query "<question>"` when graphify-out/graph.json exists. Use `graphify path "<A>" "<B>"` for relationships and `graphify explain "<concept>"` for focused concepts. These return a scoped subgraph, usually much smaller than GRAPH_REPORT.md or raw grep output.
- If graphify-out/wiki/index.md exists, use it for broad navigation instead of raw source browsing.
- Read graphify-out/GRAPH_REPORT.md only for broad architecture review or when query/path/explain do not surface enough context.
- After modifying code, run `graphify update .` to keep the graph current (AST-only, no API cost).

## Sprint 21 Phase 21.3 — Admin Lab Candidate Queue UI (2026-06-11)

**Branch:** `feature/sprint-21-lab-candidate-queue`
**Tag:** `sprint-21-phase-21-3-lab-candidate-queue`

### What was added

- Read-only `LabCaseCandidate` queue for Admin Lab
- Routes: `lab-case-candidates.index` / `lab-case-candidates.show` under `/lab/case-candidates`
- `LabCaseCandidatePolicy` (viewAny + view with branch isolation)
- Registered in `RepositoryServiceProvider.$policies`
- `LabCaseCandidateController` — branch-scoped, filter by status, search
- Views: `resources/views/lab/case-candidates/index.blade.php` and `show.blade.php`
- Sidebar: "Kandidat Lab RME" item, gated by `view_lab_orders | manage_lab_orders`

**Sprint 21 Phase 21.4 — Convert LabCaseCandidate to LabOrder (2026-06-11):** Branch `feature/sprint-21-candidate-to-laborder`. Tag `sprint-21-phase-21-4-candidate-to-laborder`. `LabCaseCandidateConversionService` converts pending candidates to `LabOrder` with explicit `lab_service_id` (no treatment mapping). Route `lab-case-candidates.convert`. Idempotent via row lock + `converted_lab_order_id`. Reuses `create_lab_orders`/`manage_lab_orders`. No lab payment/invoice records. RME payment still does not auto-create LabOrder. 16 tests in `LabCaseCandidateConversionTest.php`.

**Sprint 21 Phase 21.5 — RME Lab Workflow Polish (2026-06-11):** Branch `feature/sprint-21-rme-lab-workflow-polish`. Tag `sprint-21-phase-21-5-rme-lab-workflow-polish`. UI/visibility only — no business logic changes to payment, generation, or conversion. RME invoice/receipt show lab candidate status; candidate queue/show improved for converted state; LabOrder show displays Sumber RME when from candidate. Relations: `RmeInvoice::labCaseCandidates()`, `LabOrder::rmeLabCaseCandidate()`. 16 tests in `RmeLabWorkflowPolishTest.php`.

**Sprint 21 Phase 21.6 — RME PDF Export / Print Hardening (2026-06-11):** Branch `feature/sprint-21-rme-pdf-print-hardening`. Tag `sprint-21-phase-21-6-rme-pdf-print-hardening`. Print/PDF hardening only — visit print bundle includes patient/visit/branch, initial treatment, finalized RM + handwriting (SOAP hidden), odontogram, paid invoice/payment, lab workflow summary. Receipt print includes lab workflow panel. PDF route `rme.visits.pdf` uses existing `barryvdh/laravel-dompdf`. No payment/generation/conversion changes. 21 tests in `RmePdfPrintHardeningTest.php`.

**Sprint 21 Phase 21.7 — VPS Pilot Deployment Checklist (2026-06-11):** Branch `feature/sprint-21-vps-pilot-checklist`. Tag `sprint-21-phase-21-7-vps-pilot-checklist`. Documentation/runbook only — no deployment performed. Checklist: `docs/sprint_21_vps_pilot_deployment_checklist.md`. VPS deploy target: branch `feature/sprint-21-rme-pdf-print-hardening`, tag `sprint-21-phase-21-6-rme-pdf-print-hardening`, commit `327e55f`. **VPS rules:** always backup DB before pull/migrate; use `php artisan migrate --force` only; **never** `migrate:fresh` or `db:wipe` on VPS; reset `storage`/`bootstrap/cache` permissions after deploy.

**Sprint 21 Phase 21.8 — Sprint 21 Closure / Release Candidate Merge Plan (2026-06-11):** Branch `feature/sprint-21-closure-rc-plan`. Tag `sprint-21-phase-21-8-closure-rc-plan`. Documentation/release planning only — no merge, no VPS deployment, no app behavior changes. Closure plan: `docs/sprint_21_closure_release_candidate_plan.md`. Recommended functional RC baseline: branch `feature/sprint-21-vps-pilot-checklist`, commit `18d2eec`, tag `sprint-21-phase-21-7-vps-pilot-checklist`. VPS deployment must follow Phase 21.7 checklist (`docs/sprint_21_vps_pilot_deployment_checklist.md`) with baseline `18d2eec`. Deferred RC tag: `sprint-21-release-candidate` (after stakeholder approval). Pre-Sprint 21 rollback: `sprint-20-rme-core-ui-complete` / `48c9fe6`. **Never** `migrate:fresh` or `db:wipe` on VPS.

**Sprint 21 VPS pilot deployed + Phase 21.9 hotfix (2026-06-11):** Release branch `release/sprint-21-rme-advanced-workflow` is live on Hostinger VPS pilot. Latest hotfix commit `3ef3fd6` ("Add Kasir RME sidebar menu hotfix") is baseline for Sprint 22. Tags: `sprint-21-release-candidate`, `sprint-21-phase-21-9-cashier-rme-sidebar-hotfix`.

**Sprint 22 Phase 22.0 — Planning & Baseline (2026-06-11):** Branch `feature/sprint-22-planning`. Tag `sprint-22-planning`. Planning doc: `docs/sprint_22_planning.md`. Documentation only in Phase 22.0.

**Sprint 22 Phase 22.1 — Pilot Role/Permission/Menu Hardening (2026-06-12):** Branch `feature/sprint-22-role-permission-menu-hardening`. Tag `sprint-22-phase-22-1-pilot-role-permission-menu-hardening`. New permissions `view_owner_dashboard`, `view_branch_dashboard`. New roles **Owner**, **Kasir**, **Perawat** with least-privilege pilot assignments. Doctor hardened (no lab/cashier). Dashboard route + sidebar Dasbor gated. Tests: `RolePermissionHardeningTest`, `SidebarPermissionVisibilityTest`, `PilotRouteAuthorizationTest`. Full suite: 1940 passed. No payment/generation/conversion/schema changes. Next: Phase 22.2 RME smoke-test data.

**Sprint 58.7 — RME Registered Patient Queue Page / Antrian Pasien (2026-06-23):** Branch `feature/sprint-58-7-rme-registered-patient-queue-page`. Spec: `docs/sprint_58_7_rme_patient_queue_spec.md`. Dedicated post-registration queue for Admin Klinik at `GET /rme/patient-queue` → `rme.patient-queue.index` (`ClinicVisitController@patientQueue`), gated by the existing `view_clinic_visits|manage_clinic_visits` RME group. Queue = active non-terminal visits (status not in `cashier_pending/completed/cancelled`) across the active RME branch set, including with/without room. New service `ClinicVisitService::registeredQueue()` + repo `queueForBranches()` (+ interface). View `resources/views/rme/patient-queue/index.blade.php` reuses the Sprint 58.6 `rme.visits.assign-room` route for the per-row room selector (no duplicate logic; same-branch validation unchanged). Sidebar "Antrian Pasien" added under RME. Filters: search, room status (Belum/Sudah dipilih), status, date. No schema/permission/payment changes. Tests: `tests/Feature/RME/PatientQueueTest.php` (14 passed). `/rme/visits` + treatment worklist preserved.

**Sprint 59 — Editable RME & Table-Based Odontogram Workflow (2026-06-25):** Branch `feature/sprint-59-editable-rme-table-based-odontogram-workflow`. Removes the finalization edit-lock from both Rekam Medis and Odontogram so doctors can revise any visit (including old/finalized ones) — `status`/`finalized_at`/`finalized_by` columns are kept for backward compatibility (no data deleted). Key changes: (1) `MedicalRecordService::updateDraft()` and `MedicalRecordHandwritingController::store()` no longer block finalized records; `updateDraft` writes only submitted keys (partial saves never blank untouched fields). (2) `OdontogramService::updatePlaceholder()` + `OdontogramPolicy::update()` drop the `isFinalized()` gate. (3) Prev/next visit arrow navigation on RM + Odontogram pages via new partial `resources/views/rme/visits/partials/visit-nav-arrows.blade.php`, backed by `ClinicVisitService::adjacentVisits()` → repo `adjacentVisitsForPatient()` (chronological by `visit_date,id`, RME-branch scoped, same-patient only; RM nav requires an existing medical record so it never 404s). (4) Table-first odontogram: `odontogramEditor` Alpine component (`resources/js/app.js`) gains `availableTeeth`/`addRow()`/`setStatus()`/`removeRow()`; the doctor edits via the "Hasil Odontogram yang Dipilih" table and the FDI grid is the auto-generated visual (no manual drawing) — also rendered in detail/print views. (5) RM writing area expanded to a large always-editable "Catatan Rekam Medis" textarea (`min-h-[24rem]`, mapped to `notes`). (6) Billing manual treatment: cashier create page clarifies treatment dropdown is optional and `description` accepts free-text — already supported by the existing nullable `treatment_id` + required `description` columns, so **no migration**. No new routes (reuses `rme.visits.medical-record.update`, `rme.odontograms.update`, `*.show`). Tests: new `tests/Feature/RME/Sprint59EditableWorkflowTest.php` (7); updated 8 lock-assertion tests to assert editable-after-final. Suites green: MedicalRecord 116, Odontogram 140, RME dir 673, Cashier 87, Billing 47; pint + `git diff --check` clean. Finalize still requires handwriting; full-payment-only and SOAP-hidden rules unchanged.

## Active Planning — Sprint 60 RME Multi-Page Handwriting Canvas

* Spec file: `docs/sprint_60_rme_multi_page_handwriting_canvas_spec.md`
* Status: **SPEC / PLANNING ONLY — not implemented yet.**
* Core rule: **1 canvas = 1 RM page.**
* One `MedicalRecord` can have multiple RM pages/canvases.
* Page 1 uses **locked read-through** backward compatibility from legacy `trx_medical_record_handwritings.handwriting_path`.
* Do **not** backfill legacy Page 1 during initial implementation.
* New additive table (`trx_medical_record_handwriting_pages`) is intended for **Page 2+**.
* Editor UX: read-only page preview → click/touch → same-page overlay editor → save → preview updates.
* KTP must remain hidden.
* Do **not** target `main`; continue using base branch: `feature/sprint-26-phase-26-8-stabilization-closure-go-watch-no-go-report`.

**Hotfix Sprint 60.8 — RME Room Assignment Gate Before Doctor Examination (2026-06-26):** Branch `hotfix/rme-room-assignment-gate-before-exam` (base `feature/sprint-26-phase-26-8-stabilization-closure-go-watch-no-go-report`; do NOT target main). Baseline before: Sprint 60.7 merge `b24f7d7`. Workflow: Pendaftaran → Antrian → **Input Ruangan** → diperiksa dokter → Consent → Kasir. Room assignment is a **queue-stage** requirement, not registration-stage. **No migration** — `clinic_room_id` (nullable FK, indexed) already exists on `trx_clinic_visits` since Sprint 58.6. Changes: (1) `ClinicVisit` adds `PRE_EXAM_STATUSES` (registered/waiting/in_progress), `hasAssignedRoom()`, `isTerminal()`, `requiresRoomBeforeExam()` (roomless AND pre-exam — cashier_pending & terminal are EXEMPT so Sprint 59 post-exam editing of finalized/old visits is never blocked). (2) New route-middleware `App\Modules\ClinicVisit\Middleware\EnsureVisitRoomAssigned` (alias `visit.room` in `bootstrap/app.php`) gates RM + Odontogram routes (`rme.visits.medical-record.show|store|update|finalize|handwriting`, `rme.visits.odontogram.show`, `rme.odontograms.update|finalize`); a roomless active visit redirects to `rme.visits.show` with flash `error` = "Pasien belum ditempatkan ke ruangan perawatan." — enforced at controller/middleware level, not just hidden buttons. (3) Reuses existing `rme.visits.assign-room` route/`assignRoom()`; service now rejects room reassignment on terminal visits and adds `activeRoomsForBranch()` (visit-branch-scoped, MAIN never selectable). (4) UI: visit detail shows "Menunggu Penempatan Ruangan" banner + inline branch-scoped room selector and locks RM/Odontogram buttons when roomless; patient-queue adds the waiting badge; cashier handoff (Sprint 60.7) shows room name or "Menunggu Ruangan / Belum siap diperiksa" (repo eager-loads `clinicRoom`). KTP stays hidden. **Test-fixture change:** `ClinicVisitFactory` now defaults `clinic_room_id` to a `ClinicRoom::factory()` (roomless tests pass explicit `null`); `ClinicRoomFactory` dropped `unique()` on its 5-name pool (random suffix already unique) so per-visit room creation does not overflow. Production registration (`ClinicVisitService::create`) still leaves new visits roomless. No payment/consent/invoice/receivable/SOAP changes; full-payment-only preserved; no JS/CSS asset change (no `npm run build` needed). Tests: new `tests/Feature/RME/RmeRoomAssignmentGateTest.php` (17). Green: RME dir 736, ClinicVisit 81, MedicalRecord 121, Odontogram 144, ClinicRoom master 14, DoctorCashierSyncQueue 15; pint + `git diff --check` clean.

## Sprint 61.0 — Patient Data Completeness Audit & RM Gap Review

Status: GO CANDIDATE FOR PR REVIEW

Summary:
- Added RME patient data completeness audit page.
- Added patient completeness scoring, missing-field classification, KTP masking, duplicate-risk detection, and RM gap review.
- Added CSV export without exposing full KTP.
- Added branch filter limited to active RME-enabled branches.
- MAIN branch is excluded from audit filters.
- Added routes:
  - GET /rme/patients/audit
  - GET /rme/patients/audit/export
- No migration required.
- No full KTP rendered or exported.

Validation:
- PatientDataCompletenessAuditTest: 13 passed
- PatientRmGapReviewTest: 5 passed
- php artisan test --filter=Patient: 160 passed
- php artisan test tests/Feature/RME: 754 passed
- vendor/bin/pint --test: passed
- git diff --check: clean
- npm run build: passed

## Sprint 62.0 — Owner KPI Dashboard (2026-06-27)

Branch `feature/sprint-62-owner-kpi-dashboard-implementation` (base `feature/sprint-26-phase-26-8-stabilization-closure-go-watch-no-go-report`, HEAD `0860db9`; do NOT target main). **Enhanced** the existing Owner dashboard (route `dashboard` → `HomeDashboardController`, gated `view dashboard|view_owner_dashboard`) — did NOT create a duplicate `/owner/dashboard` route. New executive, period-based KPI block alongside the existing "today snapshot" RME/Lab pilot section. New service `App\Modules\Reporting\Services\OwnerDashboardKpiService` (period `today|7d|month|30d|custom`, default `month`, + existing `branch_id` filter): 10 KPI cards, per-branch table, daily visit/payment trends (PHP day-grouping for PG/SQLite portability), privacy-safe latest-receivables, operational alerts, permission+`Route::has`-guarded drilldowns. New partial `resources/views/dashboards/owner-kpi.blade.php` included in `dashboard.blade.php`. Reuses `view_owner_dashboard` (Supervisor RME excluded by design — no permission change). Inventory low-stock/value via `InventoryAnalyticsRepositoryInterface` (try/catch → "Belum tersedia"). No KTP/NIK, no scanned docs, no raw medical notes, no HR. **No route/permission/migration added.** Tests: `tests/Feature/Owner/OwnerKpiDashboardTest.php` (12). Green: Owner 12, Dashboard+Auth 83; pint + `git diff --check` clean.

## Sprint 62.1 — RME Doctor-to-Cashier Completion Gate (2026-06-27)

Branch `feature/sprint-62-1-rme-doctor-cashier-completion-gate` (base `feature/sprint-26-phase-26-8-stabilization-closure-go-watch-no-go-report`, HEAD `5ccf253`; do NOT target main). Enforces the RME pipeline `Pendaftaran → Antrian → Input Ruangan → Pemeriksaan Dokter → Consent → Selesai Pemeriksaan (cashier_pending) → Kasir / Pembayaran → Selesai Visit (completed)`. **No new status, no migration, no permission, no route.** Key rule: the doctor/front office can only advance a visit to `cashier_pending` ("Selesai Pemeriksaan"); `completed` ("Selesai Visit") is reachable ONLY from `cashier_pending` and in practice only via `RmePaymentService` after the invoice is settled. Changes: (1) `ClinicVisit::VALID_TRANSITIONS` removes the `in_progress → completed` edge (now `in_progress → {cashier_pending, cancelled}`). (2) `TransitionStatusRequest` drops `completed`, adds `cashier_pending`. (3) `ClinicVisitService::transitionStatus()` guards `→ completed` from any non-`cashier_pending` status with "Visit belum dapat diselesaikan total karena pembayaran belum selesai." (4) `RmeInvoiceService::create()` billing gate message → "Pembayaran belum dapat diproses karena pemeriksaan dokter belum selesai." (5) `ClinicVisitController` flashes "Pemeriksaan selesai, pasien masuk ke kasir." on the cashier handoff; `RmePaymentController` flashes "Pembayaran selesai, visit selesai total." on full payment. (6) `resources/views/rme/visits/show.blade.php` labels `cashier_pending` as "Selesai Pemeriksaan" and no longer renders a manual `completed` button. Consent gate (`CreateRmePaymentRequest`), room gate (`visit.room`), full-payment-only, SOAP-hidden, and KTP/scanned-doc/raw-note privacy all unchanged. Supervisor RME can access but cannot bypass gates (server-side). Owner KPI dashboard read-only and unchanged. Tests: new `tests/Feature/RME/RmeDoctorCashierCompletionGateTest.php` (14); updated `ClinicVisitTest` (3 old-workflow transition tests). Green: RME dir 801, ClinicVisit/CashierBilling/MedicalRecord/RmePayment regression 322, OwnerKpi+SupervisorRme+Permission 223; pint + `git diff --check` clean.

## Hotfix — RME Partial Payment Completes Visit (2026-06-27)

Branch `hotfix/rme-partial-payment-completes-visit` (base `feature/sprint-26-phase-26-8-stabilization-closure-go-watch-no-go-report`, HEAD `3ced441`; do NOT target main). **Corrects** the Sprint 62.1 partial-payment rule: **bayar sebagian tetap menyelesaikan visit**. Any successful cashier payment (`amount > 0`) now completes the current visit; the remaining balance stays active piutang and is collected at a future follow-up, instead of holding the old visit open at `cashier_pending`. **No status/migration/permission/route added.** Mapping: no payment → invoice `UNPAID`, visit `cashier_pending`; partial → invoice `PARTIAL`, visit `completed`, remaining = active piutang; full → invoice `PAID`, visit `completed`, no piutang; zero-grand-total/free control → unchanged (settles on `remainingAmount() == 0`). Change: `RmePaymentService::completeVisitIfPaid()` → `completeVisitAfterCashierPayment()` transitions `cashier_pending → completed` on `PAID` **or** `PARTIAL` (still guarded by `status === cashier_pending`); partial flash → "Pembayaran sebagian berhasil, visit selesai dan sisa tagihan masuk piutang." Sprint 62.1 gates preserved — completion runs only inside `pay()` after payable/consent/room asserts; doctor still cannot mark `completed` directly (`in_progress → completed` edge stays removed); Supervisor RME cannot bypass. Receivable list + Owner KPI key off **invoice** status/remaining (not visit status), so completed partial visits still show as piutang; zero-grand-total/zero-remaining stay excluded. Tests: `RmeDoctorCashierCompletionGateTest` (15), updated stale partial assertions in `RmePaymentTest` + `RmeControlVisitReceivableCarryOverPaymentTest`. Green: RME dir 802, CashierBilling 33, RmePayment 19, OwnerKpiDashboard+Receivable 129, SupervisorRme+Permission 212; pint + `git diff --check` clean.

## Sprint 62.2 — Patient Outstanding Receivable Carry-over for New Visits (2026-06-27)

Branch `feature/sprint-62-2-patient-outstanding-receivable-carryover` (base `feature/sprint-26-phase-26-8-stabilization-closure-go-watch-no-go-report`, HEAD `150d2d2`; do NOT target main). Spec: `docs/sprint_62_2_patient_outstanding_receivable_carryover_spec.md`. Lets the cashier see + **optionally** collect a patient's previous outstanding receivables during **any** new RME visit — **without merging old debt into the new invoice** (each `trx_rme_invoice` keeps its own status/ledger/grand_total). **No migration, no permission, no route, no table/column.** New on `RmeControlReceivableService`: `getOutstandingReceivablesForPatientVisit(visit, currentInvoice)` (patient's other UNPAID/PARTIAL invoices, RME branches, grand_total>0, remaining>0, !=current, oldest-first by visit_date,id) + `getVisitPayableSummary(currentInvoice, selectedIds[])` whose `selected_receivables` is always the **server-side intersection** of eligible ∩ submitted ids (the IDOR boundary — forged/other-patient/other-branch/paid ids are dropped). New `RmePaymentService::allocateVisitPayment()` (sibling of `allocateControlPayment`, reuses shared private helpers): single `DB::transaction`+`lockForUpdate`, FIFO selected prior receivables (oldest-first, branch+payable re-asserted under lock) → current invoice, one `trx_rme_payment` per real invoice id under a shared `payment_batch_uuid`. New `completeVisitAfterCashierBatch()` generalizes the partial-payment hotfix: any payment in the batch completes the current `cashier_pending` visit, unfilled balances stay active piutang. **Control-chain path (`getControlPayableSummary`/`allocateControlPayment`) deliberately UNCHANGED** (control keeps its stricter "own invoice must settle" completion; ordinary path uses "any payment completes"). Controller `create()` adds `outstandingReceivables` (ordinary only); `store()` routes to `allocateVisitPayment` when server-recomputed `selected_remaining>0`, else plain `pay()`/`allocateControlPayment`. `CreateRmePaymentRequest` adds nullable `selected_receivable_ids[]` and caps amount at `total_payable` when receivables selected. UI: opt-in "Piutang Sebelumnya" card in `payment/create.blade.php`, **unchecked by default**, Alpine live total, **no KTP/NIK/scans/raw notes**. Owner KPI/receivable report unchanged (per-invoice remaining, counted once, branch-attributed). Sprint 62.1 doctor→cashier + consent + room + branch gates preserved server-side. Tests: new `tests/Feature/RME/PatientOutstandingReceivableCarryOverTest.php` (20). Green: new 20, control carry-over 35, RmePayment 19, CashierBilling 33, Receivable 138, OwnerKpiDashboard 12, SupervisorRme 8, Permission 212; pint + `git diff --check` clean.

## Sprint 62.3 — Legacy RME Patient Batch Import (2026-06-27)

Branch `feature/sprint-62-3-legacy-rme-patient-batch-import` (base `feature/sprint-26-phase-26-8-stabilization-closure-go-watch-no-go-report`, HEAD `298a440`; do NOT target main). Spec: `docs/sprint_62_3_legacy_rme_patient_batch_import_spec.md`. Safe, auditable, rollback-safe batch import of legacy **patient master data only** via a staging → preview → commit workflow. **Nothing writes to `mst_patients` until explicit Commit; no visit/medical-record/invoice/payment/consent/odontogram/scanned-doc created** (historical visits deferred). Advisory columns (Ruangan, Tindakan Awal, Keluhan Utama, TTD Dokter/Pasien) are **staged only — never promoted to RME**. **Migrations (additive, `migrate` only — never `migrate:fresh`/`db:wipe`):** `stg_legacy_patient_import_batches`, `stg_legacy_patient_imports`, nullable indexed `mst_patients.import_batch_id` FK, and **`mst_patients.doctor_id` relaxed to nullable** (unresolved legacy doctor → `doctor_id = NULL`; mirrors Sprint 23.9.1 `clinic_id` relaxation; non-destructive). New `App\Modules\Patient\Models\LegacyPatientImportBatch` (status `uploaded|validated|committing|committed|rolled_back|failed`) + `LegacyPatientImportRow` (status `valid|warning|error|committed|skipped|rolled_back`); `App\Modules\Patient\Services\LegacyPatientImportService` (`parseAndStage`/`validateAndMapRow`/`commit`/`rollback`/`discard`/`templateRows`/`errorReportRows`); `LegacyPatientImportController` + `UploadLegacyPatientCsvRequest`. CSV parse mirrors `ProductImportService` (native fgetcsv, BOM strip, header assert, blank-row skip — **no maatwebsite/excel**). RM via `PatientMedicalRecordNumberService::composeForRegistration` (year from Timestamp, manual verbatim). **Hard block (error):** composed-RM collision incl. soft-deleted, KTP collision incl. soft-deleted, in-file dup RM/KTP. **Soft warning:** name+DOB match, unresolved doctor, unmappable gender, age/DOB mismatch. Commit = single `DB::transaction` + `lockForUpdate` RM/KTP re-check (preview→commit race → row `skipped`, never overwritten); idempotent (committed batch re-commit = no-op). Rollback soft-deletes the batch's patients (`import_batch_id` + `committed_patient_id`), **blocked when any imported patient has a downstream visit/RM**; pre-commit discard soft-deletes staging only. **KTP/NIK masked everywhere** (`maskKtp`) — full value only in JSON payload columns, never rendered/exported. clinic_id left NULL (branch-anchored). Routes `settings.patients.import.{index,template,store,show,errors,commit,rollback,destroy}` under existing `manage patients` group (**no new permission**); views `settings/patients/import/{create,preview}`; sidebar "Impor Pasien Legacy". MAIN excluded; branch set = active + RME-enabled. No Sprint 62.1/62.2 payment/receivable logic changed; no RME gate weakened. Tests: new `tests/Feature/Patient/LegacyPatientBatchImportTest.php` (24). Green: new 24, Patient 236, ClinicVisit 82, MedicalRecord 121, Permission 212, RME dir 822; pint + `git diff --check` clean.

## Sprint 63.1 — Structured Odontogram Print Template Conversion (2026-06-28)

Branch `feature/sprint-63-1-structured-odontogram-print-template-conversion` (base `feature/sprint-26-phase-26-8-stabilization-closure-go-watch-no-go-report`, HEAD `16bed2f`; do NOT target main). Spec: `docs/sprint_63_1_structured_odontogram_print_template_conversion_spec.md`. **Presentation-only** — renders already-saved structured odontogram data (`trx_odontograms.tooth_map_payload`) into the Daengtisia print/PDF layout. **No handwriting/canvas/OCR/AI (the earlier 63.1 canvas concept stays cancelled), no migration, no data mutation, no external dependency, no payment/receivable change, KTP/NIK never rendered.** New read-only `App\Modules\Odontogram\Services\OdontogramPrintFormatter` builds FDI visual jaw rows (center→outer), delegates DMF-T to `Odontogram::dmftCounts()` (D=caries, M=missing, F=filling/crown/root_treated; normal/untouched/unknown excluded — unknown status → default cell, never inflates), and one-row-per-tooth ascending GIGI/DIAGNOSA/PERAWATAN/DOKTER rows. New shared **dompdf-safe** partial `resources/views/rme/visits/odontogram/partials/structured-print-template.blade.php` renders header(optional)/visual/DMF-T/legend/table with **HTML `<table>` cells — NO flexbox** (continuation via `thead{display:table-header-group}` + `tr{page-break-inside:avoid}`). Standalone print (`rme.odontograms.print` → `OdontogramController@print`) and the combined visit bundle (`rme.visits.print`/`rme.visits.pdf` via `ClinicVisitController::resolvePrintViewData()` → `print-body`) both consume the same formatter + partial; the redundant `odontogram-selected-results.blade.php` was removed. Generated on demand; nothing stored. No new route/permission. Tests: new `tests/Feature/RME/StructuredOdontogramPrintTemplateTest.php` (11). Green: Odontogram filter 155, RME dir 833, print/merge/pdf regression 55; pint + `git diff --check` clean. Pending review — no PR/merge/tag/deploy.

## Sprint 64.0 — Patient-Centric RM Workspace with Swipeable Sheets (2026-06-28)

Branch `feature/sprint-64-0-patient-centric-rm-workspace-swipeable-sheets` (base `feature/sprint-26-phase-26-8-stabilization-closure-go-watch-no-go-report`, HEAD `5b60450`; do NOT target main). Spec: `docs/sprint_64_0_patient_centric_rm_workspace_swipeable_sheets_spec.md`. Visit-centric RM navigation → patient-centric workspace: **1 pasien = 1 buku RM = banyak lembar RM; 1 lembar = 1 kunjungan (UNIQUE(clinic_visit_id) preserved — exactly one sheet per visit this sprint).** No data deleted; no payment/consent/room/odontogram/per-visit-print regression; **KTP/NIK never rendered**; no HR scope; no new route/permission. **Migration (additive only — `migrate`, never `migrate:fresh`/`db:wipe`):** `2026_06_28_100001_add_workspace_columns_to_trx_medical_records_table` adds nullable indexed `canonical_visit_id`, `source_visit_id`, `sheet_number` to `trx_medical_records` (no drops/NOT NULL/backfill; legacy rows keep NULLs). New `App\Modules\MedicalRecord\Services\PatientRmWorkspaceResolver` (read-side, scoped to `BranchService::rmeEnabledIds()`, MAIN excluded): canonical anchor = earliest non-cancelled RME visit **with an MR** (visit_date,id) so the canonical URL always renders a real sheet; `sheetsForPatient()` chronological; `resolveSourceVisit()` IDOR-validated (same patient + RME branch, else null); `nextSheetNumber()`/`pickCanonicalVisitId()`/`stampWorkspaceFields()` (lazy no-op backfill). `MedicalRecordController@show` resolves canonical and **302-redirects any non-canonical visit to the canonical URL with `?source_visit_id=`** (deterministic → no loop); the canonical page renders the workspace where active sheet = `?sheet=` → source visit's sheet → canonical sheet → first, and the **active sheet's own visit drives the header + update/finalize/handwriting forms** so finalize still transitions only that visit (Sprint 62.1 doctor→cashier gate intact). `createDraft` stamps canonical/source/sheet_number; `updateDraft`/`finalize` lazily backfill. Within-sheet canvas pagination (`?rm_page=`, Sprint 60) is scoped to the active sheet and linked via the canonical URL. New partial `resources/views/rme/visits/partials/rm-sheet-nav.blade.php` ("Buku RM Pasien"): sheet tabs + prev/next + **touch swipe** (no new JS dep) + "Tambah Lembar RM" (only when source visit has no sheet) + opened-from-later-visit notice; active sheet persisted via URL `?sheet=` + `sessionStorage` (`rmws:{patientId}:sheet`/`:scroll`); read-only for view-only users. **Model `MedicalRecord`** adds the three columns (`sheet_number` int cast) + `canonicalVisit()`/`sourceVisit()` relations (`clinicVisit()` unchanged). Tests: new `tests/Feature/RME/PatientCentricRmWorkspaceTest.php` (17); updated 3 Sprint 59 RM-nav tests, 1 Sprint 60 multipage nav assertion, 1 ClinicVisit control-RM test (`followingRedirects()`). Green: RME dir 857, CashierBilling 33, Odontogram 162, MR/print regression 163; pint + `git diff --check` clean. Pending review — no PR/merge/tag/deploy.

## STORAGE-1 — Object Storage Readiness (2026-07-05)

Branch `feature/storage-1-object-storage-readiness` (base `feature/sprint-26-phase-26-8-stabilization-closure-go-watch-no-go-report`; PR #179 squash-merged, merge commit `d8a9c73`). GO tag `storage-1-object-storage-readiness-go` @ `d8a9c73`. Deployed to VPS pilot; smoke PASS. Foundation-only, **OFF by default** — no existing file moved, no disk made public, no secret logged/committed. New `object` filesystem disk (`config/filesystems.php`, driver `s3`, `visibility: private`) reading only `OBJECT_STORAGE_*` env (all added to `.env.example` blank/safe, `OBJECT_STORAGE_ENABLED=false`). New `config/object_storage.php` (`enabled`/`disk`/`required_env`/`healthcheck_prefix`). New `App\Support\Storage\ObjectStorageReadinessService` (read-only check + optional non-destructive write/read/delete healthcheck) + command `php artisan storage:object-readiness-check` (`--write-test`, `--json`, `--strict`; disabled → `disabled_ready`, exit 0). New `App\Services\Foundation\StorageGovernanceService` publishes **STORAGE-R001..R005** (upload-through-abstraction, private-by-default, non-destructive-healthcheck, no-secret-leakage, no-destructive-migration-without-plan) into `architecture:foundation-governance-summary` as an informational `storage_governance` section (not wired into the blocking `combinedDecision`). **Gotcha**: rule text must never contain the literal substring `.env` — the release-evidence safety scan (`config/release_evidence.php` `forbidden_patterns`) bans it and will fail `NSF-10 Release Evidence Gate`/CI once the rules array is captured inside `foundation-governance-summary.json`; write "the environment example file" instead. Added `league/flysystem-aws-s3-v3` (had to re-resolve `composer.lock` under a temporary `platform.php=8.3.x` override — the local dev machine's newer PHP otherwise locks a `symfony/filesystem` version requiring PHP ≥8.4, which breaks `composer install` on CI/VPS PHP 8.3). `config/foundation_roadmap.php` STORAGE-1 marked `completed` (next recommended: `STATELESS-1`); updated 4 existing roadmap-assertion tests accordingly. Docs: `docs/architecture/storage-object-readiness.md`, `docs/architecture/storage-governance-rules.md`. Tests: `Storage1GovernanceIntegrationTest` (5), `ObjectStorageReadinessCommandTest` (5). Full suite green: 3988 passed. VPS: pre-deploy backup `pre_auto_deploy_20260705-062522.sql` (621KB) via `scripts/deploy-vps.sh`; `storage:object-readiness-check` → `disabled_ready` on VPS.

## STATELESS-1 — Runtime Statelessness & Deploy Portability Foundation (2026-07-05)

Branch `feature/stateless-1-runtime-statelessness-deploy-portability-foundation` (base `feature/sprint-26-phase-26-8-stabilization-closure-go-watch-no-go-report`; PR #180 squash-merged, merge commit `c0cff0c`). GO tag `stateless-1-runtime-statelessness-deploy-portability-foundation-go` @ `c0cff0c`. Deployed to VPS pilot; smoke PASS. Foundation-only readiness audit — **no session/cache/queue/filesystem/log driver changed, no file moved, no migration**. New `config/runtime_portability.php` reads `STATELESS_*` env (all added to `.env.example` safe/no-secret, `STATELESS_READINESS_ENABLED=true`, `STATELESS_ALLOWED_LOCAL_WRITE_PATHS=storage,bootstrap/cache`). New `App\Support\Runtime\RuntimePortabilityReadinessService` (read-only): reports session/cache/queue/filesystem/log driver + app env/debug, verifies the 5 required writable paths, flags risky drivers for horizontal scale (session/cache `file`, queue `sync` → `warning` status, non-blocking exit 0 by default — acceptable for single VPS pilot), surfaces STORAGE-1's `object_storage` status without enabling it, and supports an optional non-destructive write/read/delete healthcheck. Command `php artisan runtime:stateless-readiness-check` (`--json`, `--strict`/`--fail-on-warning`, `--write-test`) + thin alias `php artisan deploy:portability-check`. New `App\Services\Foundation\StatelessGovernanceService` publishes **STATELESS-R001..R008** (no new local-ephemeral durable files, restricted writable paths, session/cache/queue auditability, scheduler/worker idempotency guardrail before scale-out, explicit cache rebuild on deploy, env-only secrets, centralized-logging roadmap for scale, non-destructive readiness/smoke commands) into `architecture:foundation-governance-summary` as an informational `stateless_governance` section (not wired into the blocking `combinedDecision`; `storage_governance`/STORAGE-R001..R005 unchanged). `config/foundation_roadmap.php` STATELESS-1 marked `completed` (next recommended: `LB-1`); updated 4 existing roadmap-assertion tests accordingly (`FoundationRoadmapGovernanceTest`, `Nsf9ReleaseSafetyGovernanceTest`, `Dbperf2GovernanceIntegrationTest`, `Storage1GovernanceIntegrationTest`). Docs: `docs/architecture/runtime-statelessness-readiness.md`, `docs/architecture/deploy-portability-governance-rules.md`. Tests: `Stateless1GovernanceIntegrationTest` (7), `RuntimeStatelessReadinessCheckCommandTest` (5). Full suite green: 4000 passed. VPS: pre-deploy backup `pre_stateless_1_runtime_portability_20260705-074849.sql` (622KB); on VPS the actual pilot drivers are session=`database`, cache=`file`, queue=`database` — readiness check reports `warning` (cache is local-disk) with exit 0, matching the "PASS with warning for single VPS pilot" design; `storage:object-readiness-check` still `disabled_ready`; `architecture:foundation-governance-summary` combined decision `GO`.

## LB-1 — Load Balancer Pilot (2026-07-05)

Branch `feature/lb-1-load-balancer-pilot` (base `feature/sprint-26-phase-26-8-stabilization-closure-go-watch-no-go-report`; PR #181 squash-merged, merge commit `1be6f50`). GO tag `lb-1-load-balancer-pilot-go` @ `1be6f50`. Deployed to VPS pilot; smoke PASS. Foundation/readiness-only — **no real multi-node traffic switch, no runtime driver forced, no file moved, no migration**. New `config/load_balancer.php` reads `LB_*` env (all added to `.env.example` safe/no-secret, `LB_TRUSTED_PROXIES=` empty by default). `bootstrap/app.php` wires an **opt-in** `$middleware->trustProxies(at: ...)` from `LB_TRUSTED_PROXIES` — skipped entirely when empty, so Laravel's stock `TrustProxies` (already in the framework's default global middleware stack) behavior is untouched by default. New minimal unauthenticated route `GET|HEAD /health/lb` (`health.lb` → `App\Http\Controllers\LoadBalancerHealthController`) always returns `{"status":"ok","service":"daengtisiams","check":"lb"}` (or `"fail"`/503 if the optional `LB_HEALTH_DEEP_CHECK` DB ping fails) — never APP_KEY/DB host/env/route list/user data/KTP/NIK/stack traces; disabled via `LB_HEALTH_ENDPOINT_ENABLED=false` (route absent, 404). New `App\Support\LoadBalancer\LoadBalancerReadinessService` (read-only): reports trusted-proxy config, forwarded-header/HTTPS/secure-cookie expectations vs actual `APP_URL` scheme/`session.secure`, session/cache/queue drivers, STORAGE-1 object storage enabled flag, STATELESS-1 runtime readiness status; also exposes `headerSmoke()` which simulates forwarded-header resolution **in-process** (synthetic proxy IP added to a copy of the trusted list for the simulation only — no real network call, no config mutation), skipped with a reason when `LB_TRUSTED_PROXIES` is empty. Command `php artisan lb:readiness-check` (`--json`, `--strict`/`--fail-on-warning`, `--header-smoke`, `--url=` reserved). Status enum `single_vps_ready|lb_pilot_ready|warning|fail`; "multi-node expected" signal is `LB_STRICT`, not merely `LB_EXPECT_FORWARDED_HEADERS` (a single VPS behind nginx already expects forwarded headers without running multiple instances). New `App\Services\Foundation\LoadBalancerGovernanceService` publishes **LB-R001..R010** (explicit trusted proxy required, no raw-IP trust without forwarded-header consideration, minimal/non-sensitive health endpoint, HTTPS/proxy verified before secure-cookie changes, shared session/cache/queue for real multi-node, non-destructive smoke, explicit cache rebuild/rollback, request-id roadmap before big multi-node traffic, governance summary shows LB without weakening other chains, no public diagnostic endpoint exposing internal state) into `architecture:foundation-governance-summary` as an informational `lb_governance` section (not wired into the blocking `combinedDecision`; `storage_governance`/`stateless_governance` unchanged). `config/foundation_roadmap.php` LB-1 marked `completed` (next recommended: `REPLICA-1`); updated 5 existing roadmap-assertion tests accordingly (`FoundationRoadmapGovernanceTest`, `Nsf9ReleaseSafetyGovernanceTest`, `Dbperf2GovernanceIntegrationTest`, `Storage1GovernanceIntegrationTest`, `Stateless1GovernanceIntegrationTest`). Docs: `docs/architecture/load-balancer-pilot-readiness.md`, `docs/architecture/load-balancer-governance-rules.md`. Tests: `Lb1GovernanceIntegrationTest` (7), `LoadBalancerReadinessCheckCommandTest` (8), `HealthLbEndpointTest` (4). Full suite green: 4019 passed. VPS: pre-deploy backup `pre_lb_1_load_balancer_pilot_20260705-085410.sql` (623KB); on VPS `lb:readiness-check` → `single_vps_ready`/`GO` (trusted proxies not configured, as expected for this pilot); `/health/lb` returns the minimal JSON with and without forwarded headers (no trusted proxy configured, so headers are ignored by design); `storage_governance`/`stateless_governance`/`lb_governance` all `GO`; roadmap `next_recommended_sprint` = `REPLICA-1`; no new Laravel log errors from this deploy.

## OBS-1 — Request ID, Correlation ID & Observability Foundation (2026-07-05)

Branch `feature/obs-1-request-id-correlation-observability-foundation` (base `feature/sprint-26-phase-26-8-stabilization-closure-go-watch-no-go-report`; PR #184 squash-merged, merge commit `df07d90`). GO tag `obs-1-request-id-correlation-observability-foundation-go` @ `df07d90`. Deployed to VPS pilot; smoke PASS. Foundation-only — **no external monitoring vendor, no log channel/session/cache/queue driver change, no migration**. New `config/observability.php` reads `OBSERVABILITY_*` env (all added to `.env.example` safe, `OBSERVABILITY_TRUST_INBOUND_REQUEST_ID=false`, `OBSERVABILITY_TRUST_INBOUND_CORRELATION_ID=false`, `OBSERVABILITY_LOG_USER_ID=false`, `OBSERVABILITY_LOG_BRANCH_ID=false`). New `App\Http\Middleware\AttachRequestCorrelationContext` registered globally on the `web` middleware group (prepended in `bootstrap/app.php`): generates/validates a bounded `request_id`/`correlation_id` (inbound `X-Request-ID`/`X-Correlation-ID` only trusted when the matching trust flag is on **and** the value passes strict length/character validation — otherwise replaced with a generated UUID), attaches a minimal PII-free log context via `Log::withContext()` (request_id, correlation_id, method, path, route — each individually toggleable; user_id/branch_id off by default), and sets the `X-Request-ID` response header. New `App\Support\Observability\ObservabilityReadinessService` (read-only; detects middleware registration by resolving `Illuminate\Contracts\Http\Kernel` first — needed because Artisan/console context never builds the router's `web` middleware group otherwise) + command `php artisan obs:readiness-check` (`--json`, `--strict`/`--fail-on-warning`, `--header-smoke` — in-process simulation, no real network call). New `App\Services\Foundation\ObservabilityGovernanceService` publishes **OBS-R001..OBS-R012** (bounded request/correlation id, inbound trust only with strict validation, PII-free log context, sensitive-data masking, non-destructive readiness command, minimal health endpoints, multi-node/queue propagation roadmap, no PII to external vendors without review, prod debug off, governance summary non-regression, PII/secret masking statement required for future logging) into `architecture:foundation-governance-summary` as an informational `observability_governance` section (not wired into the blocking `combinedDecision`; `storage_governance`/`stateless_governance`/`lb_governance`/`database_replica_governance`/`cache_redis_governance`/`cache_governance` unchanged). `config/foundation_roadmap.php` **not touched** — OBS-1 is not yet a tracked roadmap entry (roadmap `next_recommended_sprint` stays `REPLICA-1`); this is intentional, not an oversight. Docs: `docs/architecture/request-correlation-observability-readiness.md`, `docs/architecture/observability-governance-rules.md`. Tests: `Obs1GovernanceIntegrationTest` (5), `ObservabilityReadinessCheckCommandTest` (7), `RequestCorrelationMiddlewareTest` (7). Full suite green: 4066 passed, 11 skipped. VPS: pre-deploy backup `pre_obs_1_request_correlation_observability_20260705-114402.sql` (626KB); on VPS `obs:readiness-check` → `observability_ready`/GO (middleware detected, no warnings); an invalid/overlong inbound `X-Request-ID` against `/health/lb` does not break the request and a fresh generated id is returned instead; `storage_governance`/`stateless_governance`/`lb_governance`/`database_replica_governance`/`cache_redis_governance`/`cache_governance`/`observability_governance` all `GO`; no new Laravel log errors from this deploy.

## OBS-2 — Centralized Logging & Error Tracking Readiness (2026-07-05)

Branch `feature/obs-2-centralized-logging-error-tracking-readiness` (base `feature/sprint-26-phase-26-8-stabilization-closure-go-watch-no-go-report`, HEAD before this sprint `b81ee16`). Foundation-only, readiness for a future centralized log collector + error tracker — **no external monitoring vendor installed, no external log/error traffic sent, no Laravel log channel/stack changed, no migration**. New `config/observability_pipeline.php` reads `OBS_PIPELINE_*`/`CENTRAL_LOGGING_*`/`ERROR_TRACKING_*` env (all added to `.env.example` safe/no-secret; `CENTRAL_LOGGING_ENABLED=false`, `ERROR_TRACKING_ENABLED=false`, `CENTRAL_LOGGING_SEND_PII=false`, `ERROR_TRACKING_SEND_PII=false`, `ERROR_TRACKING_SAMPLE_RATE=0.0`, `OBS_PIPELINE_FAIL_ON_PII_RISK=true`). New `App\Support\Observability\CentralizedObservabilityReadinessService` (read-only; wraps OBS-1's `ObservabilityReadinessService` plus storage/stateless/LB/replica/Redis readiness for context) reports central-logging/error-tracking enabled/driver/**boolean-only** endpoint-or-DSN-configured/API-key-configured/send-PII/sample-rate, and never prints the actual endpoint/API-key/DSN value. A detected PII-send risk (`send_pii=true` with `fail_on_pii_risk` default true) always fails readiness regardless of `--strict`. Command `php artisan obs:pipeline-readiness-check` (`--json`, `--strict`/`--fail-on-warning`, `--log-smoke` — writes one safe synthetic non-PII local log line, `--error-smoke` — opt-in/default-skipped, only builds an in-process synthetic exception + local log line, never throws unhandled, never calls out). New `App\Services\Foundation\ObservabilityPipelineGovernanceService` publishes **OBS-R013..OBS-R024** (central logging/error tracking OFF by default until endpoint+privacy-review+rollback plan, no PII/secret in external export, DSN/API-key/endpoint only from env/config and never in output/docs/logs/tests, OBS-1 request/correlation id must accompany every exported event, synthetic smoke stays non-PII/bounded/safe, prod debug off before external error tracking, retention/access-control/redaction policy required before live rollout, observability tooling never changes critical RME/payment/inventory/visit-completion flow, readiness command non-destructive with no default external traffic, governance summary non-regression, vendor-adding sprints need a privacy note in PR evidence, log/error sampling configurable) into `architecture:foundation-governance-summary` as an informational `observability_pipeline_governance` section. **Deliberately kept as a separate service from `ObservabilityGovernanceService`** (OBS-1, OBS-R001..R012 unchanged, same test assertions untouched) — mirrors the existing `cache_governance` vs `cache_redis_governance` separation pattern. Not wired into the blocking `combinedDecision`; `storage_governance`/`stateless_governance`/`lb_governance`/`database_replica_governance`/`cache_redis_governance`/`cache_governance`/`observability_governance` all unchanged. `config/foundation_roadmap.php` **not touched** (same intentional non-tracking as OBS-1). Docs: `docs/architecture/centralized-logging-error-tracking-readiness.md`, `docs/architecture/observability-pipeline-governance-rules.md`. Tests: `Obs2GovernanceIntegrationTest` (6), `ObservabilityPipelineReadinessCheckCommandTest` (12). Full suite green: 4084 passed, 11 skipped. pint + `git diff --check` clean.

## ROADMAP-1 — Foundation Roadmap Canonicalization (2026-07-05)

Branch `feature/roadmap-1-foundation-roadmap-canonicalization` (base `feature/sprint-26-phase-26-8-stabilization-closure-go-watch-no-go-report`, HEAD before this sprint `5cb632c`). Governance/config/docs/test only — **no runtime driver change, no migration, no business workflow change, no new external dependency**. **Important naming note:** `config/foundation_roadmap.php` already existed as a much older, source-locked "National Foundation Expansion Roadmap" (its own `roadmap_id` is literally `'ROADMAP-1'`, tag `roadmap-1-national-foundation-expansion-source-lock-go`, predating STORAGE-1) — this sprint does **not** replace that file/service/command; it fixes staleness inside it and adds a new, explicitly disambiguated `ROADMAP-1-CANONICALIZATION` entry (`disambiguates: 'ROADMAP-1'`) so the two are never conflated. Root cause fixed: `REPLICA-1`'s entry was still `status: 'planned'` even though it shipped and was GO-tagged, which made the existing `architecture:foundation-roadmap-check`'s `next_recommended_sprint` stale at `REPLICA-1`. Changes to `config/foundation_roadmap.php`: `REPLICA-1` → `completed` (+ `governance_section`/`readiness_command`/`go_tag`/`go_commit` evidence fields, also added retroactively to `STORAGE-1`/`STATELESS-1`/`LB-1`); five new `approved_sequence` entries inserted at priority 12–16 — `CACHE-1-REDIS-READINESS` (completed, disambiguated from the older priority-3 `CACHE-1` cache-strategy/invalidation-governance entry via `disambiguates`/`disambiguation_note` on both sides), `OBS-1` (completed), `OBS-2` (completed), `ROADMAP-1-CANONICALIZATION` (completed, this sprint), `MON-1` — *Health Monitoring, Alerting & Uptime Readiness* (planned, the new next sprint); `PART-1`/`SEARCH-1`/`NDA-1`/`RC-1` renumbered to priority 17–20 (metadata-only, `RC-1.depends_on` extended to include the five new ids). `next_recommended_sprint` now resolves to `MON-1`, never a completed sprint. New `App\Services\Foundation\FoundationRoadmapGovernanceService` publishes **ROADMAP-R001..R010** (every GO-tagged/deployed sprint must be in the roadmap; next sprint never stale; completed+governance-tracked entries need `go_tag` evidence; duplicate/similar names must be disambiguated; new-rule sprints register a governance section; roadmap check stays non-destructive; roadmap is not the sole source of truth over code/tests; roadmap updates stay governance/config/docs only; deploy evidence needs tag/commit/backup/smoke/rollback; canonicalization must never regress sibling foundation governance) into `architecture:foundation-governance-summary` as a new, additive `roadmap_governance` section (existing `roadmap` section/tests, backed by the untouched `App\Services\Architecture\FoundationRoadmapService` and `architecture:foundation-roadmap-check`, are unchanged) — informational only, not wired into the blocking `combinedDecision`. New thin command `php artisan foundation:roadmap-check` (`--json`, `--strict`, `--fail-on-warning`) wraps the existing `FoundationRoadmapService`/`FoundationRoadmapGovernanceService` (no duplicate validator) and reports `completed_sprints`/`current_sprint`/`next_recommended_sprint`/`stale_next_detected`/`missing_metadata`/`governance_sections_expected`/`governance_sections_known`/`warnings`/`recommendations` — read-only, no network/GitHub API call. Updated 5 existing tests that hardcoded the stale `next_recommended_sprint === 'REPLICA-1'` expectation (`FoundationRoadmapGovernanceTest`, `Storage1GovernanceIntegrationTest`, `Stateless1GovernanceIntegrationTest`, `Lb1GovernanceIntegrationTest`, `Dbperf2GovernanceIntegrationTest`, `Nsf9ReleaseSafetyGovernanceTest`) — the same kind of update every prior foundation sprint (STORAGE-1/STATELESS-1/LB-1) also made. Docs: `docs/architecture/foundation-roadmap-canonicalization.md`, `docs/architecture/foundation-roadmap-governance-rules.md`. Tests: new `FoundationRoadmapCanonicalizationTest` (12). Full suite green: 4096 passed, 11 skipped; pint + `git diff --check` clean. Next recommended sprint after ROADMAP-1: **`MON-1` — Health Monitoring, Alerting & Uptime Readiness**.
